# قواعد کار روی این مخزن

این فایل را پیش از هر تغییری بخوان. منبع کامل تصمیم‌ها `DECISIONS.md` است؛ اینجا فقط قواعد غیرقابل‌مذاکره آمده. هر جا شمارهٔ ADR آمده، جزئیاتش در همان فایل است.

## سیاق پروژه

پنل مدیریت مستقل برای کارکنان نفس‌لند، روی API پرتال (`api.imcloud.org/site/api/v1/manage`). بک‌اند ASP.NET Core، فرانت Next.js + TypeScript + shadcn + Tailwind + React Query، پایگاه داده SQL Server. **محصولات مال ما نیستند؛ منبع حقیقت پرتال است.**

## معماری

- Modular Monolith با مرز عمودی (ADR-004). هیچ ماژولی به کلاس داخلی ماژول دیگر رفرنس نمی‌دهد — فقط به `Contracts` آن. `internal` جدی گرفته می‌شود.
- هر ماژول schema دیتابیس خودش را دارد (`identity.`، `audit.`، `catalog.`، `approvals.`) با مهاجرت جداگانه.
- ارتباط بین ماژول‌ها با رویداد درون‌فرآیندی، نه فراخوانی مستقیم سرویس.
- هر ماژول `IModule` را پیاده می‌کند و خودش را ثبت می‌کند (ADR-005). کلید permission به‌صورت const داخل خود ماژول، نه enum مرکزی.
- هر فیچر یک پوشه: Command، Handler، Validator، Endpoint (ADR-006).

## قواعدی که نقض‌شان باگ است

1. **همهٔ commandها از pipeline عبور می‌کنند:** `Logging → Validation → Authorization → Transaction → Audit → Handler`. لاگ یا چک دسترسی داخل کنترلر ننویس.
2. **پیش‌فرض بسته:** commandی که permission تعریف‌شده ندارد، رد می‌شود.
3. **چک دسترسی روی permission است، نه نام نقش.** هرگز `[Authorize(Roles = "...")]` (ADR-001).
4. **پاسخ `GET` پرتال را مستقیم در بدنهٔ `PUT` نگذار.** `categories`، `filters` و `images` در خواندن شیء و در نوشتن شناسه/رشته‌اند. انتقال مستقیم یعنی پاک شدن بی‌صدای دادهٔ محصول (ADR-027).
5. **`PUT` محصول جایگزینی کامل است.** ویرایش سطح محصول = خواندن کامل، ادغام، نوشتن بدنهٔ کامل. ارسال بدنهٔ ناقص ممنوع (ADR-017).
6. **قیمت و موجودی از `PATCH` واریانت عوض می‌شوند**، نه از `PUT` محصول (ADR-017).
7. **همهٔ تماس‌های خروجی از محدودکنندهٔ نرخ عبور می‌کنند.** سقف پرتال ۲ درخواست در ثانیه به ازای IP است؛ نرخ هدف ما ۱.۵ (ADR-026). هیچ مسیر میان‌بری نساز.
8. **توکن پرتال هرگز به مرورگر نمی‌رسد** و هرگز لاگ نمی‌شود (ADR-007، ADR-039).
9. **هیچ رمز، توکن یا رشتهٔ اتصالی در گیت نیست.** User Secrets در توسعه، متغیر محیطی در استقرار.
10. **AuditLog فقط append است.** هیچ اندپوینت update یا delete، برای هیچ نقشی (ADR-009).
11. **در محیط توسعه، نوشتن فقط روی شناسهٔ محصول تستی مجاز است** و بک‌اند بقیه را رد می‌کند (ADR-029).
12. **تاریخ‌ها UTC ذخیره می‌شوند** و مبنای خواندن از پرتال فیلد `timestamp` است، نه رشتهٔ شمسی. شمسی فقط در نمایش (ADR-035).
13. **قیمت عدد خام است.** هیچ ضرب و تقسیمی؛ واحد تومان فقط برچسب نمایشی (ADR-027).
14. **ورودی عددی نرمال می‌شود** (ارقام فارسی و عربی، جداکنندهٔ هزارگان) در یک نقطهٔ مشترک، نه در هر فرم.
15. **HTML ورودی در دو نقطه پاک‌سازی می‌شود:** نمایش در پنل و پیش از ارسال به پرتال. `text-align` و `font-weight` حفظ می‌شوند (ADR-033).
16. **اگر ادمین فیلد توضیحات را ویرایش نکرد، رشتهٔ اصلی عیناً برگردانده می‌شود** (ADR-033).
17. **خطاها ProblemDetails با `correlationId`اند.** پیام خام پرتال هرگز به کاربر نشان داده نمی‌شود (ADR-036).
18. **عملیات ایجاد کلید idempotency دارند** و درج کلید با محدودیت یکتایی انجام می‌شود، نه «اول بخوان بعد بنویس» (ADR-034).
19. **مهاجرت خودکار در استارتاپ اجرا نمی‌شود**؛ فقط بررسی می‌شود که معوق نمانده باشد (ADR-041).
20. **تست خودکار علیه پرتال واقعی اجرا نمی‌شود.** همه‌چیز روی fake (ADR-038).

## سبک کار

- کامیت‌ها Conventional Commits با بدنه‌ای که **دلیل** تغییر را توضیح می‌دهد، نه فهرست فایل‌ها.
- رابط کاربری فارسی و راست‌به‌چپ از ابتدا. وضعیت‌های انگلیسی پرتال (`pending`، `featured`، `most`) با برچسب فارسی نمایش داده می‌شوند.
- تایپ‌های فرانت از OpenAPI بک‌اند تولید می‌شوند؛ دستی تعریف نکن.
- zod منبع مشترک اعتبارسنجی فرانت، آینه‌شده در بک‌اند با FluentValidation.
- بعد از mutation فقط invalidate؛ **بدون optimistic update** (منبع حقیقت بیرونی است).
- پیش از افزودن کتابخانه یا تغییر یک تصمیم ثبت‌شده، اول در `DECISIONS.md` بررسی کن که چرا آن‌طور تصمیم گرفته شده.
