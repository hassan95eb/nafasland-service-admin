services:
  mssql:
    restart: unless-stopped
    # No port is exposed to the host; only compose's internal network can reach it.

  api:
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Production
