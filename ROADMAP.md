# نقشهٔ راه پنل مدیریت نفس‌لند

این فایل ترتیب اجرای کار را تعیین می‌کند. منبع تصمیم‌ها `DECISIONS.md` است و این فایل فقط می‌گوید هر تصمیم کِی پیاده شود. هر گام یک برش عمودی کامل است — از فرم تا پرتال — نه یک لایهٔ افقی.

## اصول ترتیب

- **عمودی، نه افقی.** «همهٔ موجودیت‌ها، بعد همهٔ سرویس‌ها» ضدالگوی Vertical Slice در ADR-006 است.
- **ریسک زودتر.** پرریسک‌ترین بخش این پروژه یکپارچگی با پرتال است، نه مدل داده. مدل دادهٔ ما کوچک است: کاربر/نقش/permission، AuditLog، ApprovalRequest، IdempotencyRecord.
- **زیرساخت مشترک پیش از اولین مصرف‌کننده‌اش.** به‌طور مشخص: Auditing باید پیش از اولین نوشتن Catalog آماده باشد، وگرنه اولین اندپوینت بی‌لاگ می‌ماند (هشدار ADR-006).
- **خواندن پیش از نوشتن.** تا جای ممکن کار روی فروشگاه واقعی بی‌خطر بماند.

---

## گام ۰ — اسکلت راه‌رونده

**هدف:** اثبات اینکه تصمیم‌های زیرساختی با هم کار می‌کنند، پیش از آنکه چیزی رویشان ساخته شود.

ساختار سولوشن (ADR-004)، `IModule` و ثبت خودکار (ADR-005)، pipeline با همهٔ behaviorها (ADR-006)، ProblemDetails و CorrelationId (ADR-036)، Serilog و `/health` (ADR-042)، اتصال SQL Server و اولین مهاجرت (ADR-019، ADR-041)، Options با اعتبارسنجی در استارتاپ (ADR-039)، مسیر `/api/v1` و feature flag و job runner (ADR-012).

**معیار پذیرش:** یک اندپوینت نمونه که از کل pipeline عبور می‌کند؛ نبود permission تعریف‌شده باعث رد شدن درخواست می‌شود؛ خطا به‌صورت ProblemDetails با `correlationId` برمی‌گردد؛ `/health` سبز است؛ مهاجرت معوق باعث بالا نیامدن برنامه می‌شود.

**انجام نده:** هیچ منطق محصولی، هیچ تماسی با پرتال.

## گام ۱ — Identity و مدل دسترسی

ADR-001، ۰۰۲، ۰۰۳، ۰۲۱، ۰۲۲، ۰۲۳.

کاربر، نقش، permission، `UserPermission` با Grant/Deny، فلگ `IsSuperAdminOnly`، ورود با کوکی `HttpOnly` و antiforgery، قفل پس از پنج تلاش، seed سوپرادمین با فلگ `IsProtected`، صفحهٔ دسترسی‌های کاربر.

**معیار پذیرش:** `AuthorizationBehavior` روی permission واقعی چک می‌کند؛ permission نشان‌دار `IsSuperAdminOnly` از رابط کاربری قابل انتساب نیست؛ محاسبهٔ permission مؤثر (نقش‌ها + Grant − Deny) تست واحد دارد.

## گام ۲ — ماژول Auditing

ADR-009، ۰۱۴، ۰۱۵.

`AuditBehavior` در pipeline، جدول فقط-append با ایندکس‌های ADR-014، job پاک‌سازی ۶ ماهه.

**معیار پذیرش:** هر command خودکار لاگ می‌شود بدون آنکه handler چیزی بنویسد؛ تلاش رد‌شده با `Outcome = Denied` ثبت می‌شود؛ هیچ مسیری برای حذف یا ویرایش لاگ وجود ندارد.

## گام ۳ — لایهٔ پرتال، فقط خواندن

ADR-007، ۰۰۸، ۰۱۷، ۰۲۶، ۰۲۷، ۰۲۸، ۰۳۸.

`IPortalProductClient`، توکن فقط در بک‌اند، محدودکنندهٔ نرخ ۱.۵ در ثانیه با صف، Polly، mapper دو طرفه، fake با پاسخ‌های ضبط‌شده.

**معیار پذیرش:** فهرست و جزئیات محصول واقعی خوانده می‌شود؛ محدودکنندهٔ نرخ قابل دور زدن نیست؛ mapper تست واحد دارد؛ تست‌ها فقط روی fake اجرا می‌شوند.

## گام ۴ — فرانت‌اند و فهرست محصولات

ADR-011، ۰۱۳، ۰۲۴، ۰۲۸، ۰۳۵.

اسکلت Next.js، ورود، سایدبار داده‌محور، فهرست با صفحه‌بندی سمت پرتال، جست‌وجو، فیلتر «بدون قیمت»، RTL از ابتدا.

**اولین ارزش قابل دیدن برای کارفرما، و هنوز بدون هیچ ریسک نوشتنی.**

## گام ۵ — مدیریت ادمین‌ها و دسترسی‌ها

ADR-001، ۰۰۲، ۰۰۳، ۰۲۱، ۰۲۲، ۰۲۳، ۰۴۶. (بک‌اندش کامل در گام ۱ ساخته شده؛ این گام فقط رویش UI می‌سازد و همان الگوی فرم/mutation را — روی داده‌ای که هیچ ریسکی برای فروشگاه واقعی ندارد — پیش از گام ۶ تمرین می‌کند.)

صفحهٔ «مدیریت ادمین‌ها» (فقط برای دارندهٔ `identity.users.manage`/`identity.access.manage` که در عمل فعلاً یعنی SuperAdmin، چون نقش `Admin` بدون permission اولیه ساخته می‌شود)، فرم ساخت حساب Admin با رمز موقت، دکمهٔ فعال/غیرفعال، دکمهٔ ریست رمز، ویرایش نقش و Grant/Deny مستقیم permissionها، جایگزینی بنر «رمز باید تغییر کند» گام ۴ با یک صفحهٔ واقعی تغییر رمز اجباری (وقتی `mustChangePassword=true` هیچ مسیر دیگری در دسترس نیست)، مخفی‌کردن آیتم منو و بستن مسیر مستقیم برای کاربر بدون permission لازم.

**معیار پذیرش:** کاربر بدون `identity.users.manage`/`identity.access.manage` نه آیتم منو را می‌بیند و نه با آدرس مستقیم به صفحه می‌رسد؛ ساخت ادمین جدید رمز موقت می‌سازد و بعد از اولین ورودش، پیش از هر صفحهٔ دیگر، فقط فرم تغییر رمز در دسترس است؛ هر عملیات (ساخت، ریست رمز، فعال/غیرفعال، تغییر نقش/permission) با شناسهٔ عامل و هدف در AuditLog ثبت می‌شود؛ تلاش یک Admin عادی برای گرفتن نقش SuperAdmin طبق ADR-046 رد می‌شود.

**باز برای بررسی هنگام پیاده‌سازی:** آیا `identity.users.manage` باید یک محدودیت صریح شبیه `IsSuperAdminOnly` هم بگیرد، تا حتی اگر یک SuperAdmin به اشتباه این permission را به یک Admin بدهد، ساخت/ویرایش ادمین‌های دیگر همچنان بسته بماند.

## گام ۶ — اولین نوشتن: قیمت و موجودی

ADR-016، ۰۱۷، ۰۳۴، ۰۳۷.

`PATCH` واریانت روی محصول تستی (ADR-029)، idempotency، تشخیص تداخل نسخه، حالت کاهش‌یافته.

**معیار پذیرش:** دو بار ارسال یک فرم یک نتیجه می‌دهد؛ تداخل نسخه `409` می‌دهد نه بازنویسی؛ محافظ شناسهٔ محصول تستی در محیط توسعه فعال است.

## گام ۷ — تست رفت‌وبرگشت `PUT`، سپس ایجاد و ویرایش محصول

اول آزمایش ADR-027 روی محصول تستی: آیا `relates`، `meta_keywords` و `canonical_url` پس از یک رفت‌وبرگشت سر جایشان می‌مانند؟ **تا نتیجه معلوم نشود، ویرایش سطح محصول شروع نمی‌شود.** سپس ADR-025، ۰۳۳ (آپلود تصویر و ویرایشگر متن).

## گام ۸ — ماژول Approvals

ADR-010، ۰۳۰، ۰۳۱، ۰۳۲. حذف محصول، انتشار، `featured`/`most`، حذف واریانت.

## گام ۹ — گزارش فعالیت

ADR-014. عمداً دیر، چون پیش از تولید داده چیزی برای نشان دادن ندارد.

## گام ۱۰ — سخت‌سازی و تحویل

ADR-040 (پشتیبان‌گیری با بازیابی آزمایش‌شده)، ADR-042 (شمارنده‌ها)، ADR-018 (استقرار لوکال)، آموزش کاربران.

---

## کارهای بیرونی که موازی پیش می‌روند

- پرسیدن اندپوینت آپلودی که مسیر `/uploads/products/` برمی‌گرداند.
- پرسیدن وجود اندپوینت مستقل ایجاد و حذف واریانت.
- تکلیف سفارش‌های گذشته هنگام حذف واریانت.
- نمونه‌برداری از ۱۵ تا ۲۰ توضیحات محصول برای تنظیم TipTap (ADR-033).
- ساخت محصول تستی و ثبت شناسه‌اش در کانفیگ توسعه (ADR-029).
