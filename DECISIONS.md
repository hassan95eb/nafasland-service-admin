# تصمیم‌های پروژهٔ پنل مدیریت نفسلند

این فایل مرجع تصمیم‌های مورد توافق پروژه است. هر تصمیم جدید پس از توافق، با تاریخ، دلیل و پیامدهای آن در همین فایل ثبت می‌شود. پیشنهادهای بررسی‌نشده نباید به‌عنوان تصمیم قطعی ثبت شوند. کلیدها، توکن‌ها و رمزها در این فایل یا Git ثبت نمی‌شوند.

## ۲۰۲۶-۰۹-۱۹ — هدف و معماری مورد توافق

- ساخت پنل مستقل برای کارکنان، با فرانت‌اند و بک‌اند جدا از سایت اصلی.
- ارتباط با نفسلند از طریق API پرتال؛ پوشش دقیق عملیات نیازمند بررسی API واقعی است.
- بک‌اند: ASP.NET Core؛ مبنای پیشنهادی قبلی .NET 10 LTS است.
- بک‌اند اختصاصی مسئول کنترل مجوزها و ارتباط با API نفسلند است؛ توکن اصلی API فقط در بک‌اند نگهداری می‌شود.
- حساب و مجوز مستقل برای کارکنان؛ امکان ایجاد و ویرایش محصول بدون مجوز حذف.
- کنترل مجوز در بک‌اند ضروری است؛ پنهان‌کردن دکمه‌ها در فرانت‌اند کنترل امنیتی کافی نیست.
- محدودیت ویرایش فیلدها و حذف دسترسی مستقیم غیرمجاز به سایت اصلی باید در طراحی لحاظ شود.

## ۲۰۲۶-۰۹-۱۹ — انتخاب فرانت‌اند

وضعیت: انتخاب مطرح‌شده توسط کاربر و مورد موافقت در این گفتگو.

- Next.js به‌جای پیشنهاد اولیهٔ Angular.
- shadcn/ui برای اجزای رابط کاربری.
- Tailwind CSS برای طراحی و استایل‌ها.
- React Query برای دریافت داده، کش و همگام‌سازی وضعیت سمت سرور.
- دلیل: ساخت رابط قابل سفارشی‌سازی برای فرم‌ها، جدول‌ها و جریان کاری کارکنان.
- انتخاب Next.js مسئولیت مجوزها و منطق کسب‌وکار را از ASP.NET Core منتقل نمی‌کند.

## ۲۰۲۶-۰۹-۱۹ — نقش‌ها و مجوزها

**Status: Superseded by ADR-001** (۲۰۲۶-۰۹-۱۹) — تعریف نقش‌ها (`SuperAdmin` و `Admin`) و محدودیت حذف محصول همچنان معتبر است، ولی مکانیزم چک دسترسی طبق ADR-001 روی permission انجام می‌شود، نه روی نام نقش. نقش صرفاً یک بستهٔ آماده از permissionهاست و در کد چک نمی‌شود.

- پنل چند کاربره است و چند ادمین به‌طور هم‌زمان از آن استفاده می‌کنند.
- نقش SuperAdmin: دسترسی کامل به همهٔ عملیات پنل.
- نقش Admin: فقط ایجاد و ویرایش محصول؛ مجوز حذف محصول را ندارد.
- نقش نویسندهٔ محتوا/وبلاگ در آینده افزوده می‌شود؛ طراحی مدل نقش و مجوز باید از ابتدا قابل توسعه باشد و افزودن نقش جدید نیازمند تغییر ساختار نباشد.
- اعمال مجوزها در بک‌اند انجام می‌شود؛ فرانت‌اند فقط رابط کاربر را متناسب با نقش نمایش می‌دهد.

## ۲۰۲۶-۰۹-۱۹ — ثبت وقایع (Audit Log)

وضعیت: نیازمندی اعلام‌شده توسط کاربر و مورد توافق در گفتگو.

- همهٔ اقدام‌های ادمین‌ها باید ثبت شود؛ این یک نیازمندی اصلی پروژه است، نه امکان جانبی.
- برای هر رویداد دست‌کم ثبت شود: کاربر انجام‌دهنده، نوع عملیات (ایجاد/ویرایش/حذف/ورود)، موجودیت و شناسهٔ هدف، زمان، و تغییر انجام‌شده (مقدار قبل و بعد).
- سوابق در بک‌اند اختصاصی نگهداری می‌شوند و از داخل پنل قابل ویرایش یا حذف نیستند.
- ثبت وقایع باید برای ادمین‌ها قابل مرور و جست‌وجو باشد (فیلتر بر اساس کاربر، تاریخ و نوع عملیات).

**یادداشت (۲۰۲۶-۰۹-۱۹، ADR-014):** عبارت «برای ادمین‌ها» در جملهٔ بالا مبهم بود. طبق ADR-014 دسترسی به گزارش فقط برای SuperAdmin است؛ ادمین هیچ نمایی از لاگ — نه مال خودش، نه مال دیگران — نمی‌بیند.

## تصمیم‌های ثبت‌شده (ADR)

از این بخش به بعد هر تصمیم به‌صورت یک ADR شماره‌دار ثبت می‌شود. ورودی‌های بالاتر دست‌نخورده باقی مانده‌اند و شماره‌گذاری از ADR-001 آغاز می‌شود.

### ADR-001 — مدل دسترسی permission-based به‌جای role-based

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** بخش‌های زیادی (محتوا، انبارداری) بعداً به پنل اضافه می‌شوند و هر بخش نقش‌های خودش را می‌آورد.

**تصمیم:**
- چک دسترسی همیشه روی permission انجام می‌شود، نه روی نام نقش.
- ساختار داده: `AppUser ──< UserRole >── Role ──< RolePermission >── Permission`.
- کلید permission به فرمت `<resource>.<action>`.
- در ASP.NET با Authorization Policy و یک `IAuthorizationHandler` که permission را از claims می‌خواند.

**دلیل:** با مدل نقش‌محور، هر نقش جدید یعنی تغییر کد و دیپلوی؛ با permission، یعنی چند ردیف داده.

**گزینهٔ رد‌شده:** `[Authorize(Roles = "Admin")]` — هزینه‌اش با نقش سوم و چهارم ترکیبی می‌شود.

### ADR-002 — حذف محصول فقط در اختیار SuperAdmin

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**
- permission با کلید `product.delete` منحصراً به نقش SuperAdmin بسته است.
- اندپوینت حذف در بک‌اند برای سایر نقش‌ها ۴۰۳ برمی‌گرداند و درخواست به API سایت اصلی فوروارد نمی‌شود.
- روی موجودیت `Permission` فلگ `IsSuperAdminOnly` اضافه می‌شود تا چنین permissionهایی از صفحهٔ مدیریت نقش‌ها قابل انتساب به نقش دیگری نباشند.

**دلیل:** مخفی‌کردن دکمه در UI کنترل امنیتی نیست. بدون این فلگ، سوپرادمین می‌تواند ماه‌ها بعد سهواً همین محدودیت را از صفحهٔ نقش‌ها دور بزند.

**پیامد:** تلاش رد‌شده برای حذف با `Outcome = Denied` در AuditLog ثبت می‌شود.

### ADR-003 — هر کاربر می‌تواند چند نقش داشته باشد

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:** `UserRole` به‌صورت many-to-many تعریف می‌شود. permissionهای مؤثر کاربر برابر است با اتحاد permissionهای همهٔ نقش‌هایش.

**دلیل:** یک نفر عملاً هم‌زمان ادمین محصول و انباردار یا محتوانویس خواهد بود.

### ADR-004 — Modular Monolith با مرز عمودی

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:** ساختار پروژه بر اساس دامنه چیده می‌شود، نه لایهٔ سراسری:

```
src/
├─ Api/                  host و Program.cs
├─ Shared/
│  ├─ Kernel/            Result<T>, IModule, IDomainEvent, base types
│  └─ Infrastructure/    auth، permission handler، audit interceptor، HTTP resilience
└─ Modules/
   ├─ Identity/          کاربر، نقش، permission، لاگین
   ├─ Auditing/          AuditLog و خواندن/فیلتر آن
   ├─ Catalog/           محصول (روی API سایت اصلی)
   ├─ Approvals/         جریان تأیید (عمومی)
   ├─ Content/           وبلاگ — آینده
   └─ Inventory/         انبار — آینده
```

قواعد الزامی:
- هیچ ماژولی به کلاس داخلی ماژول دیگر رفرنس نمی‌دهد؛ فقط به `Contracts` آن. دسترسی `internal` جدی گرفته می‌شود.
- هر ماژول اسکیمای دیتابیس خودش را دارد (`identity.`، `audit.`، `catalog.`، `inventory.`) با مایگریشن جداگانه.
- ارتباط بین ماژول‌ها با رویداد درون‌فرآیندی (مثل `ProductUpdated`) انجام می‌شود، نه فراخوانی مستقیم سرویس ماژول دیگر.

**دلیل:** Onion سراسری با پنج دامنهٔ بی‌ربط، هر لایه را به انبار پوشه‌های نامرتبط تبدیل می‌کند و تغییر در انبار، کد محصول را می‌شکند.

**گزینهٔ رد‌شده:** میکروسرویس — برای این مقیاس هزینهٔ عملیاتی بی‌دلیل است؛ مرز اسکیما جداسازی بعدی را ارزان نگه می‌دارد.

### ADR-005 — ثبت خودکار ماژول با IModule

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**

```csharp
public interface IModule
{
    void RegisterServices(IServiceCollection services, IConfiguration config);
    void MapEndpoints(IEndpointRouteBuilder app);
    IReadOnlyList<PermissionDefinition> Permissions { get; }
}
```

- `Program.cs` اسمبلی‌ها را اسکن و همهٔ `IModule`ها را رجیستر می‌کند.
- permissionها از همین‌جا جمع و در استارتاپ seed می‌شوند.
- کلید permission هر ماژول به‌صورت const داخل خودش تعریف می‌شود (`InventoryPermissions.StockAdjust`)، نه در یک enum مرکزی.

**دلیل:** اضافه‌کردن ماژول انبار باید یک پوشه باشد، نه دست‌زدن به هفت فایل. permissionهای ماژول جدید خودشان در صفحهٔ مدیریت نقش‌ها ظاهر می‌شوند.

### ADR-006 — Vertical Slice با pipeline اجباری

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**
- هر فیچر یک پوشه شامل Command، Handler، Validator و Endpoint است.
- همهٔ commandها از یک pipeline عبور می‌کنند: `Logging → Validation → Authorization → Transaction → Audit → Handler`.
- `AuthorizationBehavior` اگر روی command هیچ permission تعریف‌شده‌ای نبود، درخواست را **رد** می‌کند (پیش‌فرض بسته، نه باز).

**دلیل:** لاگ و چک دسترسی یک بار نوشته می‌شوند و هر فیچر آینده به‌طور خودکار لاگ‌دار و محافظت‌شده متولد می‌شود. لاگ داخل کنترلر یعنی اولین اندپوینت انبار که با عجله اضافه شود بی‌لاگ می‌ماند.

### ADR-007 — توکن API سایت اصلی هرگز به مرورگر نمی‌رسد

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**
- فرانت فقط با بک‌اند ASP.NET حرف می‌زند؛ بک‌اند به‌عنوان BFF/پروکسی با توکن سرویس‌اکانت به API سایت اصلی وصل می‌شود.
- سشن پنل در کوکی httpOnly نگه داشته می‌شود؛ توکن در `localStorage` ذخیره نمی‌شود.

**دلیل:** اگر فرانت مستقیم به API اصلی بزند، محدودیت حذف محصول بی‌معنی است — ادمین از DevTools خودش `DELETE` می‌زند.

### ADR-008 — لایهٔ ضد فساد (ACL) روی API پرتال

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**
- `IPortalProductClient` در `Catalog/Contracts` تعریف می‌شود، با پیاده‌سازی typed `HttpClient` + Polly (timeout، retry فقط روی عملیات idempotent، circuit breaker).
- DTOهای پرتال در مرز ماژول به مدل داخلی map می‌شوند و هرگز به کنترلر یا فرانت نمی‌رسند.
- توکن در `IPortalTokenProvider` با کش و رفرش خودکار نگهداری می‌شود.

**دلیل:** تنها بخش خارج از کنترل ما همین است؛ تغییر قرارداد پرتال باید یک mapper را بشکند، نه کل پنل.

### ADR-009 — ساختار AuditLog

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:** جدول فقط append است (هیچ اندپوینت update/delete، حتی برای SuperAdmin). فیلدها:

```
Id, CorrelationId, ActorUserId, OnBehalfOfUserId?, ActorRoleAtTime, Action,
EntityType, EntityId, BeforeJson, AfterJson, ChangedFields,
Outcome (Success/Failed/Denied), UpstreamStatus?, FailureReason?,
IpAddress, UserAgent, CreatedAt
```

قواعد:
- قبل از هر آپدیت، نسخهٔ فعلی از API سایت اصلی خوانده و در `BeforeJson` ذخیره می‌شود.
- تلاش‌های ناموفق و رد‌شده هم لاگ می‌شوند.
- نوشتن لاگ در `AuditBehavior` انجام می‌شود، نه در کنترلرها.
- همراه آن `ProductRef (ExternalProductId, LastKnownTitle, LastSyncedAt)` نگه داشته می‌شود.

**دلیل:** منبع حقیقت محصول بیرونی است؛ بدون `BeforeJson` معلوم نمی‌شود چه چیزی عوض شد، و بدون `ProductRef` لاگ‌های قدیمی به «محصول ۸۴۲۱» تبدیل می‌شوند.

### ADR-010 — ماژول عمومی Approvals برای عملیات نیازمند تأیید

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:** حذف محصول از مسیر درخواست/تأیید ثبت‌شده در سیستم انجام می‌شود. ماژول `Approvals` هیچ دانشی از دامنه‌های دیگر ندارد؛ هر ماژول یک مجری رجیستر می‌کند:

```csharp
public interface IApprovalExecutor
{
    string RequestType { get; }          // "catalog.product.delete"
    string RequiredPermission { get; }
    Task<ApprovalPreview> PreviewAsync(string payload, CancellationToken ct);
    Task<Result> ExecuteAsync(string payload, ApprovalContext ctx, CancellationToken ct);
}
```

موجودیت:

```
ApprovalRequest(Id, RequestType, TargetEntityType, TargetEntityId, PayloadJson,
SnapshotJson, Reason, Status, RequestedByUserId, RequestedAt, ReviewedByUserId?,
ReviewedAt?, ReviewNote?, ExecutedAt?, ExecutionError?, ExpiresAt?, RowVersion)
```

با `Status`: `Pending / Approved / Rejected / Executed / ExecutionFailed / Cancelled / Expired`

قواعد الزامی:
1. `PayloadJson` همان command سریالایزشده است و تأیید یعنی دیسپچ همان command از pipeline معمول. **نوع command از `RequestType` و رجیستری resolve می‌شود، هرگز از نام کلاس داخل JSON** (دیسریالایز پولیمورفیک با type name داخل payload یعنی اجرای دلخواه کد).
2. در لحظهٔ اجرا، permission تأییدکننده چک می‌شود. ادمین فقط `product.delete.request` دارد. اگر دسترسی تأییدکننده عوض شده باشد، درخواست Pending رد می‌شود نه اجرا.
3. تصمیم یک‌بار و ترمینال است؛ گذار وضعیت داخل تراکنش با `RowVersion` انجام می‌شود.
4. `Approved` و `Executed` جدا هستند. شکست تماس آپ‌استریم → `ExecutionFailed` با خطای ذخیره‌شده و تلاش مجدد دستی. **ریتری خودکار روی حذف ممنوع است.**
5. `PreviewAsync` وضعیت فعلی موجودیت را زنده می‌خواند، نه از `SnapshotJson`.
6. درخواست Pending پس از ۷ روز `Expired` می‌شود (job زمان‌بندی‌شده).

permissionها:

```
product.delete.request   → Admin
product.delete           → SuperAdmin
approval.review          → SuperAdmin
approval.read.own        → همه نقش‌ها
approval.read.all        → SuperAdmin
```

یادداشت (۲۰۲۶-۰۹-۱۹، تأیید کاربر): در حال حاضر فقط دو نقش وجود دارد — `Admin` و `SuperAdmin`. نام `ProductAdmin` استفاده نمی‌شود. ادمین هیچ مسیری برای حذف محصول ندارد و تنها می‌تواند درخواست حذف ثبت کند؛ اجرای حذف فقط پس از تأیید SuperAdmin انجام می‌شود.

اندپوینت‌ها:

```
POST   /api/v1/approvals                      ثبت درخواست (reason اجباری)
GET    /api/v1/approvals?status=&type=&mine=
GET    /api/v1/approvals/{id}                 جزئیات + preview زنده
POST   /api/v1/approvals/{id}/approval        (approval.review)
POST   /api/v1/approvals/{id}/rejection       (note اجباری)
POST   /api/v1/approvals/{id}/cancellation    فقط درخواست‌دهنده، فقط Pending
POST   /api/v1/approvals/{id}/retry           پس از ExecutionFailed
```

رویدادهای AuditLog: `ApprovalRequested`، `ApprovalApproved`، `ApprovalRejected`، `ApprovalExecuted`، `ApprovalExecutionFailed`، `ApprovalCancelled`، `ApprovalExpired` — در رکورد اجرا هم `ActorUserId` (تأییدکننده) و هم `OnBehalfOfUserId` (درخواست‌دهنده) ثبت می‌شود، با `ApprovalRequestId` در `CorrelationId`.

**دلیل:** انبارداری همین جریان را برای اصلاح موجودی لازم دارد؛ ماژول عمومی از `if (type == ...)` جلوگیری می‌کند. `ApprovalRequest` یک ماشین وضعیت است و با AuditLog که فقط append است ادغام نمی‌شود.

### ADR-011 — ساختار فرانت‌اند

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**
- Next.js App Router با route group بر اساس دسترسی (`app/(panel)/products | inventory | content | admin`)، و چک permission در layout سمت سرور قبل از رندر.
- چیدمان `features/<domain>/{api, components, hooks, schemas}` و `shared/{ui, lib, permissions, data-table, crud}`.
- ناوبری به‌صورت داده: آرایهٔ `navItems` با فیلد `permission`؛ سایدبار از روی آن رندر و فیلتر می‌شود.
- تایپ‌ها از OpenAPI بک‌اند تولید می‌شوند (`openapi-typescript`/NSwag).
- `DataTable` و الگوی `CrudPage` مشترک روی shadcn؛ zod به‌عنوان منبع مشترک فرم و اعتبارسنجی، آینه‌شده در بک‌اند با FluentValidation.
- React Query با key factory به ازای هر فیچر؛ بعد از mutation فقط invalidate، **بدون optimistic update** (منبع حقیقت بیرونی است).
- RTL و فونت فارسی از ابتدا در `globals.css` و `dir="rtl"`.
- کامپوننت `<Can permission="...">` فقط برای UI است؛ چک واقعی در بک‌اند انجام می‌شود.
- ادمین به‌جای دکمهٔ حذف، «درخواست حذف» با دلیل اجباری می‌بیند؛ محصول دارای درخواست Pending بَج می‌گیرد. صفحهٔ «درخواست‌های من» به‌همراه کارتابل سوپرادمین با بَج شماره در سایدبار.
- اعلان فعلاً بَج درون‌پنلی است، پشت پورت `INotificationSender`.

### ADR-012 — تصمیم‌های ارزان امروز، گران فردا

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:** این موارد از ابتدا پیاده می‌شوند:
- مسیر `/api/v1/` روی همهٔ اندپوینت‌ها.
- feature flag به ازای هر ماژول (برای دیپلوی ماژول نیمه‌کاره در حالت خاموش).
- background job runner: **Hangfire** (نک. ADR-045).
- Options pattern با اعتبارسنجی کانفیگ در استارتاپ به ازای هر ماژول.

**دلیل:** انبارداری بلافاصله سنکرون دوره‌ای و هشدار انقضا می‌خواهد؛ اضافه‌کردن job runner بعداً یعنی دست‌زدن به بوت‌استرپ اپ.

### ADR-013 — احراز هویت، دامنه و زمان اجرای فرانت‌اند

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**
- Next.js روی **Node runtime** اجرا می‌شود؛ خروجی static (`output: 'export'`) کنار گذاشته شد.
- **TypeScript** و **App Router** برای فرانت‌اند.
- سشن پنل در کوکی با `HttpOnly` + `Secure` + `SameSite=Strict` نگهداری می‌شود، به‌همراه توکن antiforgery روی همهٔ درخواست‌های تغییردهنده (POST/PUT/PATCH/DELETE).
- فرانت و مسیر `/api` از **یک دامنه** سرو می‌شوند؛ reverse proxy درخواست‌های `/api` را به ASP.NET Core هدایت می‌کند.

**دلیل:**
- چک permission در layout سمت سرور (ADR-011) به اجرای سروری نیاز دارد و با خروجی static ممکن نیست. با static، فیلتر منوها و مسیرها فقط بعد از لود جاوااسکریپت انجام می‌شود و ساختار مسیرهای غیرمجاز به کاربر نشان داده می‌شود.
- کوکی `HttpOnly` با جاوااسکریپت خواندنی نیست، پس یک XSS (حتی از یک پکیج آلودهٔ npm) سشن ادمین را لو نمی‌دهد؛ چیزی که دربارهٔ توکن در `localStorage` صادق نیست. هزینه‌اش باز شدن سطح حملهٔ CSRF است که با `SameSite=Strict` و توکن antiforgery بسته می‌شود.
- سرو شدن از یک دامنه اجازه می‌دهد `SameSite=Strict` واقعاً استفاده شود، CORS لازم نباشد و درخواست‌ها preflight اضافه نگیرند. دو دامنهٔ جدا `SameSite=None` را اجباری می‌کند و همان محافظتی را که در بالا خریده‌ایم پس می‌دهد.
- تایپ‌های تولیدشده از OpenAPI (ADR-011) فقط با TypeScript ارزش دارند: تغییر قرارداد API باید بیلد فرانت را بشکند، نه اینکه در پروداکشن به `undefined` تبدیل شود.

**گزینه‌های رد‌شده:** خروجی static؛ نگهداری توکن در `localStorage`؛ دو دامنهٔ جدا با CORS و `SameSite=None`.

**پیامد:** موارد متناظر از بخش «پیشنهادهای اجرایی» و سؤال باز مربوط به Node در برابر static بسته شدند.

### ADR-014 — گزارش فعالیت ادمین‌ها در پنل

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** ADR-009 ساختار دادهٔ `AuditLog` را تعریف کرد، ولی هیچ نما، مسیر یا permissionی برای خواندن آن تعریف نشده بود. مشاهدهٔ اینکه هر ادمین دقیقاً چه کرده، یک نیاز اصلی کارفرماست.

**تصمیم — دسترسی:**
- permissionهای `audit.read.all` و `audit.export` منحصراً به SuperAdmin بسته می‌شوند و هر دو با فلگ `IsSuperAdminOnly` (ADR-002) علامت می‌خورند تا از صفحهٔ مدیریت نقش‌ها به نقش دیگری قابل انتساب نباشند.
- نقش Admin هیچ نمایی از AuditLog ندارد؛ نه سوابق دیگران و نه سوابق خودش. permission ای مثل `audit.read.own` تعریف نمی‌شود.
- آیتم منوی گزارش از طریق `navItems` با `permission: "audit.read.all"` فیلتر می‌شود و مسیر آن در layout سمت سرور هم چک می‌شود (ADR-011).

**تصمیم — نماها:** ماژول `Auditing` این چهار نما را ارائه می‌دهد:

1. **لاگ سراسری** در `app/(panel)/admin/audit` — جدول همهٔ رویدادها با فیلتر روی کاربر، بازهٔ تاریخ، `Action`، `EntityType` و `Outcome` (`Success` / `Failed` / `Denied`).
2. **فعالیت هر ادمین** در `app/(panel)/admin/audit/users/{userId}` — خلاصهٔ شمارشی در بازهٔ انتخابی (تعداد ایجاد، ویرایش، درخواست حذف، تلاش رد‌شده) به‌همراه تایم‌لاین کارهای همان کاربر.
3. **دیف فیلدبه‌فیلد** در جزئیات هر رویداد — `ChangedFields` مبنای نمایش است و `BeforeJson`/`AfterJson` به‌صورت «مقدار قبل ← مقدار بعد» رندر می‌شوند، نه JSON خام.
4. **تاریخچهٔ یک محصول** از صفحهٔ خود محصول — فیلتر روی `EntityType = Product` و `EntityId`، با عنوان محصول از `ProductRef` (ADR-009) تا رکوردهای قدیمی بی‌نام نشوند.

**تصمیم — خروجی:**
- دکمهٔ خروجی CSV/Excel دقیقاً روی همان فیلترهای اعمال‌شدهٔ نما کار می‌کند.
- خود عملیات خروجی با `Action = AuditExported` و شرح فیلترها در AuditLog ثبت می‌شود.
- سقف خروجی هم‌زمان ۲۵٬۰۰۰ ردیف است؛ بیشتر از آن به job پس‌زمینه (ADR-012) سپرده می‌شود و آماده‌شدن فایل به کاربر اعلام می‌شود.

**تصمیم — پیاده‌سازی:**
- ماژول `Auditing` فقط خواندن ارائه می‌دهد؛ هیچ اندپوینت `PUT`/`DELETE` روی AuditLog وجود ندارد (ADR-009).
- صفحه‌بندی از نوع keyset روی `CreatedAt` است، نه offset.
- ایندکس‌های لازم از ابتدا: `(ActorUserId, CreatedAt DESC)` و `(EntityType, EntityId, CreatedAt DESC)`.
- `ActorRoleAtTime` در همان رکورد نمایش داده می‌شود، چون نقش کاربر ممکن است بعداً عوض شده باشد.

**محدودیت صریح (۲۰۲۶-۰۹-۱۹):** AuditLog فقط کارهایی را ثبت می‌کند که **از این پنل** انجام شده‌اند. امروز همین کاربران محصولات را از پنل خود پرتال تعریف می‌کنند و قرار است به این پنل منتقل شوند؛ طبق گفتهٔ کارفرما کسی از پنل پرتال قیمت را تغییر نمی‌دهد. پس این محدودیت عمدتاً به **دورهٔ گذار** مربوط است، ولی حذف نمی‌شود: تا وقتی دسترسی پنل پرتال باز است، نبودِ یک رویداد در گزارش به‌معنای «انجام نشده» نیست و باید در صفحهٔ گزارش گفته شود.

**دلیل:** گزارش فعالیت دلیل وجودی AuditLog است؛ بدون نما، داده‌ای جمع می‌شود که کسی نمی‌بیند. بستن دسترسی به SuperAdmin هم ساده‌ترین حالت است و هم جلوی این را می‌گیرد که ادمین‌ها کار همدیگر را زیر نظر بگیرند.

**گزینهٔ رد‌شده:** دادن `audit.read.own` به ادمین — شفافیت بیشتری نمی‌آورد و یک مسیر خواندن دیگر را باید امن نگه داشت.

### ADR-015 — مدت نگهداری و پاک‌سازی سوابق AuditLog

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**
- سوابق AuditLog به‌مدت **۶ ماه** در جدول اصلی نگهداری می‌شوند.
- پاک‌سازی رکوردهای قدیمی‌تر از ۶ ماه با یک job زمان‌بندی‌شده (ADR-012) انجام می‌شود، نه از داخل پنل. قاعدهٔ ADR-009 سر جای خود است: هیچ اندپوینتی برای حذف یا ویرایش لاگ وجود ندارد و هیچ کاربری — از جمله SuperAdmin — نمی‌تواند رکوردی را پاک کند.
- پیش از حذف، رکوردهای همان بازه در یک فایل بایگانی فشرده ذخیره می‌شوند تا پاک‌سازی به‌معنای از دست رفتن سابقه نباشد.
- خودِ اجرای پاک‌سازی با `Action = AuditPurged` و بازهٔ حذف‌شده در همان AuditLog ثبت می‌شود.

**دلیل:** بدون سقف، جدول لاگ بی‌نهایت رشد می‌کند و کوئری‌های گزارش (ADR-014) کند می‌شوند. ۶ ماه برای پیگیری اختلاف‌های کاری کافی است و بایگانی، هزینهٔ این انتخاب را می‌پوشاند.

### ADR-016 — کنترل تداخل ویرایش روی API پرتال

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** بررسی مستندات عمومی API پرتال (documenter.getpostman.com/view/1457513/S17wP6sU، سرویس `api.imcloud.org/site/api/v1`) دو چیز را روشن کرد:
- هدرهای پاسخ `GET /store/products/:id` شامل `Cache-Control: private`، `Content-Type`، `Content-Encoding`، `Vary`، `Server`، `X-Powered-By`، `Date` و `Content-Length` است. **هیچ `ETag` و هیچ `Last-Modified`ی وجود ندارد**، پس درخواست شرطی با `If-Match` ممکن نیست.
- در مقابل، خودِ موجودیت یک فیلد `version` دارد (مقداری شبیه `"636728979807370000"`، یعنی DateTime.Ticks دات‌نت). این فیلد هم روی `product` و هم روی `page` دیده می‌شود.

**تصمیم:**
- قفل خوش‌بینانه بر پایهٔ فیلد `version` خودِ موجودیت پیاده می‌شود، نه هدر HTTP.
- هنگام باز کردن فرم ویرایش، `version` همراه دادهٔ محصول در فرم نگه داشته می‌شود و در command ویرایش به بک‌اند برمی‌گردد.
- بک‌اند پیش از ارسال تغییر به پرتال، محصول را دوباره می‌خواند (همان خواندنی که ADR-009 برای `BeforeJson` لازم دارد، پس درخواست اضافه‌ای تحمیل نمی‌شود) و `version` را مقایسه می‌کند.
- اگر `version` عوض شده باشد، تغییر اعمال **نمی‌شود**؛ پاسخ `409 Conflict` برمی‌گردد و فرانت تفاوت نسخهٔ لود‌شده با نسخهٔ فعلی را به ادمین نشان می‌دهد تا خودش تصمیم بگیرد.
- تلاش ناموفق با `Outcome = Failed` و `FailureReason = VersionConflict` در AuditLog ثبت می‌شود.
- `LastKnownVersion` به `ProductRef` (ADR-009) اضافه می‌شود.
- اگر بعداً معلوم شد API مدیریتی پرتال خودش نسخه را در درخواست ویرایش می‌پذیرد و تداخل را تشخیص می‌دهد، همان مکانیزم جای این یکی را می‌گیرد و خواندن دوباره حذف می‌شود.

**دلیل:** بدون این کنترل، دو ادمین که هم‌زمان یک محصول را باز کرده‌اند، تغییر همدیگر را بی‌صدا پاک می‌کنند و در لاگ هم فقط یک ویرایش عادی دیده می‌شود. چون سرور هدر نسخه نمی‌دهد، تنها نقطهٔ اتکا فیلد `version` در بدنهٔ پاسخ است.

**به‌روزرسانی (۲۰۲۶-۰۹-۱۹، پس از بررسی کالکشن Manage):**
- `PUT /site/api/v1/manage/store/products/:id` در پاسخ `version` جدید را برمی‌گرداند (`{"success": true, "description": "...", "version": "636935987281030000"}`).
- ~~اما `GET` مدیریتی فیلد `version` ندارد~~ — **اصلاح ۲۰۲۶-۰۹-۱۹ با پاسخ واقعی:** `GET /site/api/v1/manage/store/products/:id` روی سرویس واقعی `version` را برمی‌گرداند (`"version": "639250631030770000"`). نمونهٔ مستندات قدیمی بود. پس نسخهٔ مرجع از همان خواندنی می‌آید که برای `BeforeJson` انجام می‌شود و نیازی به اندپوینت عمومی نیست.
- هیچ پارامتر یا هدری برای ویرایش شرطی در درخواست وجود ندارد؛ یعنی سرور تداخل را تشخیص نمی‌دهد و کنترل، کاملاً سمت ما است.
- واریانت‌ها (که قیمت و موجودی واقعی در آن‌هاست) اصلاً `version` ندارند.

در نتیجه مکانیزم نهایی:
- نسخهٔ مرجع از دو منبع می‌آید: پاسخ آخرین `PUT` خودمان، یا `GET` اندپوینت عمومی محصول.
- پیش از ذخیره، نسخهٔ فعلی خوانده و با نسخهٔ لود‌شده مقایسه می‌شود؛ اختلاف یعنی `409`.
- **ریسک باقی‌مانده:** بین لحظهٔ مقایسه و لحظهٔ `PUT` یک پنجرهٔ کوچک رقابتی باقی می‌ماند که با این API قابل حذف نیست. برای واریانت‌ها هم تشخیص تداخل ممکن نیست؛ کاهش ریسک از راه ارسال فیلدهای تغییرکرده با `PATCH` انجام می‌شود (ADR-017).

### ADR-017 — نگاشت عملیات پنل به API مدیریتی پرتال

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** کالکشن Manage مستندات پرتال بررسی شد (documenter.getpostman.com/view/1457513/S1LyUnDK). پایهٔ مسیرها `/site/api/v1/manage/...` است و احراز هویت با Bearer Token انجام می‌شود.

**اندپوینت‌های موجود:**

```
GET    /manage/store/products                 فهرست محصولات
GET    /manage/store/products/:id             یک محصول (بدون فیلد version)
POST   /manage/store/products                 ایجاد — پاسخ فقط { success, description, id }
PUT    /manage/store/products/:id             ویرایش کامل — پاسخ شامل version جدید
DELETE /manage/store/products/:id             حذف
GET    /manage/store/products/variants        فهرست واریانت‌ها (فیلتر product_id، page، size)
GET    /manage/store/products/variants/:id    یک واریانت
PUT    /manage/store/products/variants/:id    ویرایش کامل واریانت
PATCH  /manage/store/products/variants/:id    ویرایش جزئی: price, compare_price, shipping, tax, stock, sku, increase
GET    /manage/store/orders  |  /orders/:id  |  /orders/count  |  /orders/sum  |  /orders/report
PUT    /manage/store/orders/:id
GET    /manage/store/messages  |  /messages/:id
```

**تصمیم:**
- توکن Bearer فقط در بک‌اند نگهداری می‌شود و هرگز به مرورگر نمی‌رسد (ADR-007). `IPortalTokenProvider` در ADR-008 همین توکن را مدیریت می‌کند.
- **قیمت و موجودی از مسیر `PATCH` واریانت تغییر می‌کنند، نه از `PUT` محصول.** قیمت و موجودی واقعی روی واریانت‌ها هستند و `PATCH` فقط فیلدهای فرستاده‌شده را عوض می‌کند.
- `PUT` محصول یک **جایگزینی کامل** است، نه ویرایش جزئی. پس هر ویرایش سطح محصول به‌صورت خواندن ← ادغام ← نوشتن انجام می‌شود: بک‌اند نسخهٔ کامل فعلی را از `GET` مدیریتی می‌خواند (همان خواندنی که `BeforeJson` در ADR-009 لازم دارد)، تغییرات را روی آن اعمال می‌کند و بدنهٔ کامل را می‌فرستد. ارسال بدنهٔ ناقص ممنوع است، چون فیلدهای نفرستاده پاک می‌شوند.
- چون پاسخ `POST` فقط `id` برمی‌گرداند، پس از ایجاد یک `GET` انجام می‌شود تا `AfterJson` در AuditLog کامل باشد.
- فیلد `increase` در `PATCH` واریانت برای تغییر نسبی موجودی است؛ هر جا مقدار افزایشی/کاهشی مد نظر است از همین استفاده می‌شود تا نیازی به خواندن-نوشتن نباشد. ماژول انبار در آینده روی همین بنا می‌شود.
- اندپوینت‌های سفارش و پیام در دامنهٔ فعلی پنل نیستند. اگر بعداً اضافه شوند، ماژول جداگانهٔ خودشان را می‌گیرند (ADR-004) و از `Catalog` بیرون می‌مانند.
- همهٔ این تماس‌ها از پشت `IPortalProductClient` و لایهٔ ضد فساد (ADR-008) عبور می‌کنند؛ شکل پاسخ پرتال (`success`/`description`) به مدل داخلی map می‌شود و به فرانت نمی‌رسد.

**دلیل:** `PUT` جایگزینی کامل است و ساده‌ترین اشتباه ممکن — فرستادن فقط فیلد تغییرکرده — بی‌سروصدا بقیهٔ محصول را پاک می‌کند. `PATCH` واریانت این ریسک را برای رایج‌ترین ویرایش‌ها (قیمت و موجودی) کلاً حذف می‌کند.

**موارد نیازمند بررسی روی محیط واقعی:** پارامترهای صفحه‌بندی `GET /manage/store/products` در مستندات ذکر نشده؛ محدودیت نرخ درخواست، آپلود تصویر، و ساختار دسته‌بندی‌ها و فیلترها هنوز بررسی نشده‌اند. هدرهای پاسخ اندپوینت‌های مدیریتی هم بررسی نشد (در بخش عمومی `ETag` وجود نداشت).

### ADR-018 — محل استقرار: ابتدا سیستم لوکال شرکت

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:** پنل در مرحلهٔ اول روی سیستم داخلی شرکت مستقر می‌شود. اگر محدودیت یا مشکلی پیش آمد، به VPS منتقل می‌شود.

**پیامدها:**
- اتصال اینترنت روی همان سیستم شرط کارکرد است، چون API پرتال بیرونی است.
- انتقال بعدی به VPS نباید بازنویسی بخواهد: کانفیگ از Options pattern و متغیرهای محیطی خوانده می‌شود (ADR-012) و هیچ مسیر یا آدرسی در کد هارد‌کد نمی‌شود.
- پشتیبان‌گیری از پایگاه داده — که سوابق AuditLog در آن است — روی سیستم داخلی باید تعریف‌شده باشد، وگرنه یک خرابی دیسک یعنی از دست رفتن کل تاریخچه.

### ADR-019 — پایگاه داده: SQL Server

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)
**جایگزین:** پیشنهاد قبلی PostgreSQL کنار گذاشته شد.

**تصمیم:** SQL Server به‌عنوان پایگاه دادهٔ پنل (کاربران، نقش‌ها، permissionها، AuditLog و ApprovalRequest) استفاده می‌شود، با EF Core.

**پیامدها:**
- جداسازی اسکیمایی ماژول‌ها (ADR-004) با schemaهای `identity.`، `audit.`، `catalog.` و `approvals.` انجام می‌شود.
- `RowVersion` در ADR-010 مستقیماً روی `rowversion` بومی SQL Server می‌نشیند.
- صفحه‌بندی keyset در ADR-014 و ایندکس‌های آن بدون تغییر قابل پیاده‌سازی است.

### ADR-020 — دامنهٔ نسخهٔ اول: فقط محصولات

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:** نسخهٔ اول پنل فقط محصولات را پوشش می‌دهد: فهرست، جست‌وجو، ایجاد، ویرایش (شامل قیمت و موجودی از راه واریانت)، و درخواست/تأیید حذف. سفارش‌ها، پیام‌ها، وبلاگ و انبار در این نسخه نیستند، هرچند اندپوینت‌های سفارش و پیام در API پرتال موجودند.

**دلیل:** ماژول‌های بعدی طبق ADR-004 و ADR-005 بدون تغییر در هستهٔ برنامه اضافه می‌شوند، پس بزرگ‌کردن دامنهٔ نسخهٔ اول هیچ چیزی را ارزان‌تر نمی‌کند و فقط اولین تحویل را عقب می‌اندازد.

### ADR-021 — تعریف مجوزهای هر ادمین از رابط سوپرادمین

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**
- ادمین اجازهٔ تغییر قیمت را دارد (`product.price.update`)، و تغییر قیمت از مسیر `PATCH` واریانت انجام می‌شود (ADR-017).
- علاوه بر نقش‌ها، سوپرادمین می‌تواند برای **هر ادمین به‌صورت جداگانه** permission اضافه یا کم کند. برای این کار جدول `UserPermission(UserId, PermissionId, Effect)` با دو حالت `Grant` و `Deny` اضافه می‌شود.
- permissionهای مؤثر کاربر = (اتحاد permissionهای نقش‌هایش + Grantهای مستقیم) − Denyهای مستقیم. `Deny` بر همه‌چیز اولویت دارد.
- صفحهٔ «دسترسی‌های کاربر» در بخش مدیریت پنل، permissionهای هر ماژول را گروه‌بندی‌شده نشان می‌دهد (فهرست از `IModule.Permissions` در ADR-005 می‌آید، پس ماژول جدید خودبه‌خود ظاهر می‌شود).
- فلگ `IsSuperAdminOnly` (ADR-002) اینجا هم رعایت می‌شود: `product.delete`، `audit.read.all` و `audit.export` از این صفحه به هیچ کاربری قابل انتساب نیستند.
- هر تغییر دسترسی با `Action = PermissionChanged` و مقدار قبل/بعد در AuditLog ثبت می‌شود.

**دلیل:** نقش‌ها برای حالت کلی خوب‌اند، ولی خواستهٔ واقعی این است که یک ادمین خاص بتواند قیمت بزند و دیگری نه، بدون ساختن یک نقش جدید برای هر ترکیب. مدل permission-محور ADR-001 این را بدون تغییر کد ممکن می‌کند.

### ADR-022 — ساخت اولین SuperAdmin با seed

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:** اولین حساب SuperAdmin در استارتاپ برنامه seed می‌شود. نام کاربری و رمز اولیه از کانفیگ (متغیر محیطی) خوانده می‌شوند و در کد یا مخزن قرار نمی‌گیرند. اگر حساب از قبل وجود داشته باشد، seed کاری نمی‌کند.

- همین حساب نقش «حساب اضطراری» را دارد: با فلگ `IsProtected` علامت می‌خورد و از داخل پنل قابل حذف، غیرفعال‌سازی یا تنزل نقش نیست — حتی توسط سوپرادمین‌های دیگر. رمز آن بیرون از گردش کار روزمره نگهداری می‌شود.
- نقش سومی برای «لایهٔ بالاتر» تعریف نمی‌شود؛ SuperAdmin از قبل همه‌کاره است و افزودن نقش دیگر فقط مدل دسترسی را پیچیده می‌کند.

**پیامدها:**
- تغییر رمز در اولین ورود اجباری است.
- ساخت این حساب با `Action = SuperAdminSeeded` در AuditLog ثبت می‌شود.
- اگر سوپرادمین‌ها دسترسی همدیگر را خراب کنند یا خود را قفل کنند، ورود با همین حساب راه بازگشت است.

### ADR-023 — مدل ورود و اعتبارنامهٔ ادمین‌ها

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** ADR-013 محل نگهداری سشن را تعیین کرد (کوکی `HttpOnly`)، ولی خودِ اعتبارنامه تصمیم نشده بود.

**تصمیم:**
- رمز عبور با **Argon2id** هش می‌شود (در صورت نبود، bcrypt). هیچ الگوریتم هش عمومی مثل SHA یا MD5 استفاده نمی‌شود.
- حداقل طول رمز و اجبار تغییر رمز در اولین ورود اعمال می‌شود.
- **بازیابی رمز از راه ریست توسط سوپرادمین** انجام می‌شود، نه ایمیل یا پیامک. رمز ریست‌شده موقت است و در اولین ورود باید عوض شود.
- **قفل پس از تلاش ناموفق:** ۵ تلاش ناموفق پیاپی → قفل ۱۵ دقیقه‌ای همان حساب.
- **ورود دومرحله‌ای (TOTP)** در نسخهٔ اول فعال نیست، ولی فیلدهای لازم از ابتدا در مدل کاربر پیش‌بینی می‌شوند تا فعال‌کردن بعدی مهاجرت سنگین نخواهد.
- ورود موفق، ورود ناموفق، قفل شدن حساب و ریست رمز همگی در AuditLog ثبت می‌شوند (`Outcome` مطابق ADR-009).

**دلیل:** پنل به اینترنت وصل است، پس بدون قفل تلاش ناموفق، حدس رمز آزاد است. بازیابی از راه سوپرادمین برای تیمی در این اندازه کافی است و وابستگی به کانال اعلان — که فعلاً تصمیمش به تعویق افتاده — ایجاد نمی‌کند.

### ADR-024 — استراتژی خواندن از پرتال: ترکیبی

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**
- **جزئیات محصول و لحظهٔ ویرایش: همیشه خواندن زنده.** اینجا انتخابی وجود ندارد؛ ADR-009 برای `BeforeJson` و ADR-016 برای مقایسهٔ `version` هر دو به دادهٔ تازه نیاز دارند. کش‌کردن این مسیر یعنی بی‌اثر شدن قفل خوش‌بینانه.
- **فهرست محصولات: کش کوتاه‌مدت سمت بک‌اند (۳۰ تا ۶۰ ثانیه).**
- پس از هر نوشتن موفق، کش همان محصول و فهرست باطل می‌شود تا ادمین بلافاصله نتیجهٔ کار خودش را ببیند (همان `invalidate` بدون optimistic update در ADR-011).
- کش فقط در بک‌اند است، نه در مرورگر؛ چون کلید کش به توکن سرویس‌اکانت و دادهٔ مشترک وابسته است، نه به کاربر.

**دلیل:** منبع حقیقت بیرونی است و سایت اصلی مستقل تغییر می‌کند، پس کش طولانی خطر نمایش دادهٔ کهنه دارد. از طرف دیگر، بدون کش، هر بالا و پایین کردن فهرست ده‌ها درخواست به پرتال می‌زند — و چون محدودیت نرخ پرتال هنوز معلوم نیست، این ریسک بی‌دلیلی است.

### ADR-025 — تکلیف موقت آپلود تصویر محصول

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted) — موقت، تا پاسخ ارائه‌دهندهٔ پرتال

**زمینه:** در هیچ‌یک از سه کالکشن بررسی‌شده (Public، Manage، User) اندپوینتی برای آپلود تصویر محصول وجود ندارد. تنها آپلودهای موجود `user/avatar`، `user/documents/file` و پیوست تیکت‌اند که به حساب کاربر سایت وابسته‌اند، مسیرشان `/site/resources/...` است (نه `/uploads/products/...`) و در مورد مدارک، چرخهٔ تأیید و وضعیت `pending` دارند.

**تصمیم:**
- اندپوینت `user/documents/file` برای تصویر محصول استفاده **نمی‌شود**. سوءاستفاده از یک اندپوینت با دامنهٔ دیگر یعنی روزی که رفتار مدارک عوض شود، تصاویر محصولات می‌شکنند.
- در نسخهٔ اول، ایجاد و ویرایش محصول فقط با تصاویری کار می‌کند که از قبل در سایت اصلی موجودند؛ ادمین مسیر تصویر را انتخاب می‌کند و فیلد `image`/`images` دست‌نخورده منتقل می‌شود.
- آپلود پشت یک پورت به نام `IPortalMediaClient` تعریف می‌شود که فعلاً پیاده‌سازی «پشتیبانی‌نشده» دارد و در رابط کاربری به‌صورت پیام روشن به ادمین نمایش داده می‌شود، نه خطای مبهم.
- به‌محض مشخص شدن اندپوینت رسمی آپلود، فقط یک پیاده‌سازی پشت همین پورت اضافه می‌شود.

**به‌روزرسانی (۲۰۲۶-۰۹-۱۹ — پاسخ ارائه‌دهندهٔ پرتال):** همان اندپوینت آپلود مستندشده مسیر درست است. جریان کار تأییدشده:

1. فایل با `multipart/form-data` و فیلد `file` به اندپوینت آپلود فرستاده می‌شود.
2. پاسخ، نام/مسیر فایل ذخیره‌شده را برمی‌گرداند (`{"success": true, "path": "/site/resources/documents/443ea82c574f41b38f65eaca909c36ec.jpg"}`).
3. **همان مقدار بازگشتی، بدون هیچ بازنویسی یا حدس زدن مسیر**، در فیلد `image` یا آرایهٔ `images` محصول قرار می‌گیرد و با `PUT`/`POST` محصول ذخیره می‌شود (ADR-017).

در نتیجه بند «استفاده نمی‌شود» در بالا منتفی است و `IPortalMediaClient` پیاده‌سازی واقعی می‌گیرد، نه پیاده‌سازی «پشتیبانی‌نشده». نسخهٔ اول می‌تواند آپلود تصویر داشته باشد.

قواعد پیاده‌سازی:
- مسیر بازگشتی عیناً ذخیره می‌شود؛ هیچ‌جای کد مسیر فایل را نمی‌سازد.
- آپلود هم از محدودکنندهٔ نرخ ADR-026 عبور می‌کند، چون درخواست به همان API است.
- نوع و حجم فایل پیش از ارسال در بک‌اند اعتبارسنجی می‌شود.
- آپلود با `Action = ProductImageUploaded` در AuditLog ثبت می‌شود و مسیر بازگشتی در رکورد می‌آید.
- اگر آپلود موفق شد ولی ذخیرهٔ محصول شکست خورد، فایل یتیم روی پرتال می‌ماند؛ این حالت در لاگ ثبت می‌شود تا قابل پیگیری باشد.

**هشدار از دادهٔ واقعی (۲۰۲۶-۰۹-۱۹):** تصاویر محصولات واقعی نفس‌لند زیر `/uploads/products/...` هستند (مثلاً `/uploads/products/24fc99.webp`)، نه `/site/resources/documents/...` که اندپوینت آپلود مدارک برمی‌گرداند. پس باید تأیید شود اندپوینت آپلودی که استفاده می‌کنیم مسیری از نوع `/uploads/products/` برمی‌گرداند؛ در غیر این صورت اندپوینت درست، چیز دیگری است.

**نیازمند آزمایش با توکن واقعی:** اندپوینت آپلود در کالکشن User مستند شده و توکن Bearer آن کالکشن را می‌خواهد. باید بررسی شود که آیا توکن سرویس‌اکانت مدیریتی روی همین اندپوینت کار می‌کند یا پنل باید اعتبارنامهٔ دومی هم نگه دارد. اگر دومی لازم شد، آن هم مثل توکن اصلی فقط در بک‌اند می‌ماند (ADR-007).

### ADR-026 — رعایت محدودیت ۲ درخواست در ثانیهٔ API پرتال

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** ارائه‌دهندهٔ پرتال به‌صورت تلفنی اعلام کرد محدودیت نرخ **۲ درخواست در ثانیه** است. چون پنل با یک توکن سرویس‌اکانت واحد به پرتال وصل می‌شود (ADR-007)، این سقف بین **همهٔ** ادمین‌ها مشترک است، نه به‌ازای هر کاربر.

**تصمیم:**
- محدودیت **به ازای IP** است، نه به ازای توکن (تأییدشده توسط ارائه‌دهنده). یعنی سقف با هر ترافیک دیگری که از همان IP عمومی به پرتال می‌رود مشترک است.
- یک محدودکنندهٔ نرخ سراسری (token bucket، با نرخ پیش‌فرض ۱.۵ درخواست در ثانیه و قابل تنظیم از کانفیگ) داخل `IPortalProductClient` قرار می‌گیرد و همهٔ تماس‌های خروجی از آن عبور می‌کنند. هیچ مسیری اجازهٔ دور زدن آن را ندارد.
- درخواست‌های مازاد در صف می‌مانند، نه اینکه رد شوند. صف سقف و مهلت انتظار دارد؛ در صورت پر شدن، به کاربر پیام روشن «سامانه شلوغ است، دوباره تلاش کنید» داده می‌شود، نه خطای مبهم.
- در صورت دریافت `429`، سیاست Polly (ADR-008) با backoff نمایی و احترام به هدر `Retry-After` تلاش مجدد می‌کند — و طبق همان ADR فقط روی عملیات idempotent.
- عملیات دسته‌ای و jobهای پس‌زمینه (ADR-012) سهم جداگانه و کمتری از همین سقف می‌گیرند تا کار تعاملی ادمین‌ها را گرسنه نگذارند، و ترجیحاً خارج از ساعت کاری اجرا می‌شوند.
- کش فهرست محصولات (ADR-024) با همین سقف معنا پیدا می‌کند و TTL آن روی ۶۰ ثانیه تنظیم می‌شود.

**پیامد عملی:** هر ذخیرهٔ محصول دست‌کم دو تا سه تماس با پرتال دارد (خواندن برای `BeforeJson` و مقایسهٔ `version`، سپس نوشتن). یعنی هر ذخیره حدود یک تا یک‌ونیم ثانیه از سهم کل پنل را می‌گیرد. با چند ادمین هم‌زمان، صف محسوس می‌شود؛ رابط کاربری باید وضعیت «در حال ذخیره» را صادقانه نشان دهد و از ارسال دوبارهٔ فرم جلوگیری کند.

**چرا نرخ هدف زیر سقف تنظیم می‌شود:** با استقرار لوکال (ADR-018) همهٔ دستگاه‌های شبکهٔ شرکت پشت یک IP عمومی NAT می‌شوند. هر تست Postman، اسکریپت یا اتصال دیگری در همان دفتر از همین سهم برمی‌دارد و محدودکنندهٔ ما هیچ راهی برای دیدن آن ترافیک ندارد — فقط درخواست‌های خودش را می‌شمارد. پس ۲ تا سقفِ در اختیار ما نیست، سقف مشترک است و باید حاشیه نگه داشت.

**پیامد:** `429` یک حالت عادی است نه استثنا؛ باید با backoff مدیریت شود و پیام معنادار به ادمین بدهد، نه صفحهٔ خطا.

**محرک انتقال به VPS:** اگر `429`ها آزاردهنده شدند، این یک دلیل فنی مشخص برای انتقال به VPS است (ADR-018). با IP اختصاصی، کل سهم مال پنل است و ترافیک هیچ‌کس دیگری با آن جمع نمی‌شود.

### ADR-027 — نگاشت خواندن به نوشتن در محصول (شکل داده فرق می‌کند)

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** یک پاسخ واقعی `GET /manage/store/products/:id` از فروشگاه نفس‌لند بررسی شد. چند تفاوت با نمونهٔ مستندات و با بدنهٔ `PUT` پیدا شد که مستقیماً روی خواندن-ادغام-نوشتن ADR-017 اثر می‌گذارد.

**آنچه `GET` برمی‌گرداند در برابر آنچه `PUT` می‌خواهد:**

| فیلد | شکل در `GET` | شکل در بدنهٔ `PUT` |
|---|---|---|
| `categories` | آرایهٔ شیء: `{id, title, url, weight}` | آرایهٔ شناسه: `[178004128]` |
| `filters` | آرایهٔ شیء: `{id, title, url, weight}` | آرایهٔ شناسه: `[179221238]` |
| `images` | آرایهٔ شیء: `{path, title}` | آرایهٔ رشته: `["/uploads/products/24fc99.webp"]` |

**تصمیم:**
- نگاشت این سه فیلد از شکل خواندن به شکل نوشتن وظیفهٔ صریح mapper در لایهٔ ضد فساد (ADR-008) است و تست واحد اختصاصی دارد. انتقال مستقیم پاسخ `GET` به بدنهٔ `PUT` ممنوع است — نتیجه‌اش یا خطا است یا پاک شدن بی‌صدای دسته‌بندی‌ها و تصاویر محصول.
- مدل داخلی محصول شکل خودش را دارد و هر دو سمت به آن map می‌شوند؛ هیچ DTO پرتالی به فرانت نمی‌رسد.

**فیلدهایی که در `GET` هستند ولی در بدنهٔ مستندشدهٔ `PUT` نیامده‌اند:** `relates` (آرایهٔ شناسهٔ محصولات مرتبط)، `meta_keywords`، `canonical_url`، `stats`، `position`، `creator`.
- `relates` و `meta_keywords` روی محصولات واقعی نفس‌لند پر هستند و محتوای سئوی ارزشمندی‌اند. اگر `PUT` آن‌ها را نپذیرد یا ما نفرستیم، یک ویرایش سادهٔ قیمت می‌تواند محتوای سئوی محصول را پاک کند.
- **تا وقتی این با یک رفت‌وبرگشت واقعی آزمایش نشده، ویرایش سطح محصول پیاده نمی‌شود.** ویرایش قیمت و موجودی از `PATCH` واریانت (ADR-017) این ریسک را ندارد و می‌تواند جلوتر برود.

**نکات دیگر از همان پاسخ:**
- `category` (مفرد) `null` است در حالی‌که `categories` سه عضو دارد؛ مفرد قدیمی یا بلااستفاده است و نباید به آن تکیه شود.
- `attributes` هم `null` است در حالی‌که `filters` پر است؛ این دو یکی نیستند.
- شناسه‌های داخل `filters` به‌نظر شناسهٔ مقدار (برگ) هستند، نه گروه — در نمونه، فیلتر «ژک ساف Jacsaf» با `url: /site/pages/filter/179221238`.
- `variants` در پاسخ واقعی فیلدهای بیشتری از مستندات دارند: `product_id`، `image`، `files`.
- `slug` فارسی است و `url` به‌صورت percent-encoded برمی‌گردد؛ هیچ‌کدام در پنل بازسازی نمی‌شوند.
- تاریخ‌ها هم شمسی‌اند هم `universal`/`timestamp` (`"1405/06/22"` کنار `09/13/2026` و `1789282560`). داخل سیستم از `timestamp` استفاده می‌شود و شمسی فقط در لایهٔ نمایش ساخته می‌شود.
- **واحد قیمت تومان است** (تأییدشده ۲۰۲۶-۰۹-۱۹: `608800` روی سایت «۶۰۸٬۸۰۰ تومان» نمایش داده می‌شود). عدد خام دست‌نخورده منتقل می‌شود و «تومان» فقط برچسب نمایشی است؛ هیچ ضرب و تقسیمی روی قیمت انجام نمی‌شود.

### ADR-028 — فهرست محصولات: صفحه‌بندی سمت پرتال، بدون همگام‌سازی کامل

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** پاسخ واقعی `GET /manage/store/products` نشان می‌دهد `total: 5833` و `count: 25` — یعنی صفحه‌بندی وجود دارد و اندازهٔ پیش‌فرض ۲۵ است. با نرخ هدف ۱.۵ درخواست در ثانیه (ADR-026)، خواندن کل کاتالوگ ۲۳۴ درخواست و حدود سه دقیقه طول می‌کشد.

**تصمیم:**
- هیچ همگام‌سازی کامل کاتالوگی در پنل انجام نمی‌شود. فهرست همیشه صفحه‌به‌صفحه از پرتال خوانده می‌شود.
- صفحه‌بندی، جست‌وجو و مرتب‌سازی به پرتال واگذار می‌شود، نه اینکه در پنل روی دادهٔ کامل انجام شود.
- کش ۶۰ ثانیه‌ای ADR-024 به ازای هر ترکیب پارامتر (صفحه، اندازه، جست‌وجو، فیلتر) نگهداری می‌شود، نه روی کل فهرست.
- اگر پارامترهای `page`/`size`/`keywords`/`sorting` روی این اندپوینت پشتیبانی نشوند، این تصمیم باید بازنگری شود؛ آزمایش آن در «موارد باز» است.

**قیمت در فهرست (روشن شد ۲۰۲۶-۰۹-۱۹):** `null` بودن `price` یعنی **قیمتی برای آن محصول تعریف نشده**، نه اینکه اندپوینت قیمت نمی‌دهد. در نمونهٔ واقعی همهٔ ۲۵ محصول تازه‌ساخته هنوز قیمت‌گذاری نشده بودند. پس:
- ستون قیمت در فهرست نمایش داده می‌شود و برای مقدار `null` به‌جای صفر، «قیمت تعریف نشده» نشان داده می‌شود. نمایش صفر برای محصول بدون قیمت یک باگ گزارش‌شدنی است.
- همین وضعیت یک فیلتر مفید است: «محصولات بدون قیمت» — کاری که کاربران الان با چشم انجام می‌دهند.

**وضعیت `pending`:** در دادهٔ واقعی بیشتر محصولات `["pending", "unavailable"]` یا `["pending", "available"]` هستند، در حالی‌که محصول منتشرشده `["approved", "available", "sale"]` بود. یعنی `pending` در برابر `approved` وضعیت تأیید محتوایی است و محصولات `pending` روی سایت دیده نمی‌شوند. پنل باید این وضعیت را صریح نشان دهد و تعیین شود محصولی که ادمین می‌سازد با کدام وضعیت ثبت می‌شود.

### ADR-029 — محیط تست: یک محصول تستی مشخص روی فروشگاه واقعی

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** پرتال محیط sandbox ندارد و اکانت دومی هم در دسترس نیست.

**تصمیم:**
- یک محصول تستی مشخص روی فروشگاه واقعی ساخته می‌شود و **همهٔ** آزمایش‌های نوشتن فقط روی آن انجام می‌شود.
- این محصول در وضعیت منتشرنشده (`pending`/`unavailable`) نگه داشته می‌شود تا برای مشتری دیده نشود.
- شناسهٔ آن در کانفیگ محیط توسعه ثبت می‌شود و در محیط توسعه، هر عملیات نوشتن روی شناسه‌ای غیر از آن، در بک‌اند **رد** می‌شود. این یک محافظ کد است، نه یک قرار شفاهی.
- تست خودکار علیه پرتال واقعی اجرا نمی‌شود؛ تست‌ها روی fake داخلی اجرا می‌شوند و آزمایش با پرتال واقعی دستی و محدود به همان محصول است.

**ریسک پذیرفته‌شده:** آزمایش روی فروشگاه زنده انجام می‌شود. محافظ شناسه و وضعیت منتشرنشده این ریسک را کم می‌کند ولی صفر نمی‌کند.

### ADR-030 — انتشار محصول از مسیر تأیید سوپرادمین

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** در پرتال، `pending` در برابر `approved` تعیین می‌کند محصول روی سایت دیده شود یا نه. امروز ادمین‌ها محصولات را در پنل پرتال تعریف می‌کنند و این کار به پنل جدید منتقل می‌شود.

**تصمیم:**
- محصولی که ادمین از این پنل می‌سازد، در وضعیت `pending` ثبت می‌شود. ادمین به‌تنهایی نمی‌تواند محصولی را منتشر کند.
- انتشار (تغییر به `approved`) از **همان ماژول عمومی Approvals** (ADR-010) عبور می‌کند، با یک `IApprovalExecutor` به نام `catalog.product.publish`.
- permissionها:

```
product.publish.request  → Admin
product.publish          → SuperAdmin (با فلگ IsSuperAdminOnly)
```

- `PreviewAsync` این مجری وضعیت فعلی محصول را زنده می‌خواند و به سوپرادمین نشان می‌دهد چه چیزی قرار است منتشر شود (عنوان، قیمت، موجودی، تصاویر، دسته‌بندی‌ها).
- رویدادهای انتشار با همان رویدادهای Approvals در AuditLog ثبت می‌شوند و `ActorUserId` (تأییدکننده) و `OnBehalfOfUserId` (سازنده) هر دو ثبت می‌شوند.
- تغییر معکوس (از `approved` به `pending`) هم همین مسیر را دارد؛ خارج کردن یک محصول از دید مشتری، تصمیم سوپرادمین است.

**دلیل:** انتشار محصول مثل حذف، یک عمل قابل مشاهده برای مشتری است و برگشتش هزینه دارد. استفاده از همان ماژول Approvals یعنی صفر خط زیرساخت جدید — همان چیزی که ADR-010 برایش عمومی طراحی شد؛ این دومین مصرف‌کنندهٔ آن است و طراحی را اعتبارسنجی می‌کند.

**نکتهٔ باز:** تکلیف بقیهٔ عضوهای `status` (`available`/`unavailable`، `sale`، `featured`، `most`) جداگانه تصمیم می‌شود؛ این ADR فقط `pending` در برابر `approved` را پوشش می‌دهد.

### ADR-031 — تکلیف عضوهای `status` محصول

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:** آرایهٔ `status` محصول به سه دستهٔ دسترسی تقسیم می‌شود:

| وضعیت | معنی | چه کسی می‌زند |
|---|---|---|
| `pending` / `approved` | انتشار روی سایت | فقط از مسیر تأیید سوپرادمین (ADR-030) |
| `available` / `unavailable` | موجود بودن برای خرید | ادمین، مستقیم (`product.availability.update`) |
| `sale` | نمایش برچسب تخفیف | ادمین، مستقیم (`product.sale.update`) |
| `featured` / `most` | قرار گرفتن در صفحهٔ اول سایت | از مسیر تأیید سوپرادمین |

- مجری تأیید `catalog.product.status` برای `featured` و `most` تعریف می‌شود، از همان ماژول Approvals (ADR-010).
- این وضعیت‌ها در رابط کاربری با برچسب فارسی و توضیح کوتاه نمایش داده می‌شوند، نه با نام انگلیسی خام؛ ادمین نباید حدس بزند `most` یعنی چه.
- هر تغییر وضعیت در AuditLog با مقدار قبل و بعد ثبت می‌شود.

**دلیل:** موجود/ناموجود کردن و برچسب تخفیف، کار روزمرهٔ ادمین است و تأیید گرفتن برایش فقط کار را کند می‌کند. در مقابل، `featured` و `most` جای محصول را در صفحهٔ اول تعیین می‌کنند — یعنی تصمیم بازاریابی، نه عملیات روزمره.

### ADR-032 — دامنهٔ کار روی واریانت‌ها: ایجاد، ویرایش و حذف

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:** پنل هر سه عملیات را روی واریانت‌ها پوشش می‌دهد: افزودن، ویرایش و حذف.

**نکتهٔ مهم دربارهٔ API:** در کالکشن Manage فقط `GET`، `PUT` و `PATCH` برای واریانت مستند شده است. **هیچ اندپوینت ایجاد یا حذف واریانت وجود ندارد.** تنها راه دیده‌شده، فرستادن آرایهٔ کامل `variants` در `PUT` محصول است.

پیامدها:
- ویرایش واریانت موجود از `PATCH` انجام می‌شود و بی‌خطر است (ADR-017).
- **افزودن و حذف واریانت به همان `PUT` جایگزینی کامل محصول وابسته است**، یعنی به تست رفت‌وبرگشت ADR-027 گره خورده. تا آن تست انجام نشده، این دو عملیات پیاده نمی‌شوند.
- پیش از حذف یک واریانت، باید روشن شود بر سر سفارش‌های گذشته‌ای که به آن واریانت ارجاع دارند چه می‌آید (در پاسخ سفارش، `items[].variant.id` دیده می‌شود). اگر حذف، سابقهٔ سفارش را خراب می‌کند، حذف باید به «غیرفعال کردن» تبدیل شود.
- حذف واریانت مثل حذف محصول یک عمل بازگشت‌ناپذیر است؛ اگر آخرین واریانت محصول حذف شود، محصول عملاً بی‌قیمت و غیرقابل خرید می‌شود. بنابراین حذف واریانت هم از مسیر تأیید سوپرادمین عبور می‌کند (مجری `catalog.variant.delete` روی ماژول Approvals).

**سؤال باز برای ارائه‌دهندهٔ پرتال:** آیا اندپوینت مستقلی برای ایجاد و حذف واریانت وجود دارد که در مستندات نیامده؟

### ADR-033 — ویرایشگر متن غنی و پاک‌سازی HTML

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** فیلدهای `description` و `contents` محصول HTML هستند و در پنل پرتال با **Froala** نوشته شده‌اند (امضای `fr-original-style` در محتوای واقعی دیده می‌شود). لایسنس Froala در اختیار نیست.

**تصمیم — ویرایشگر:**
- از **TipTap** (متن‌باز) در پنل استفاده می‌شود و بازنویسی HTML توسط آن پذیرفته می‌شود، با سه شرط الزامی:
  1. **اگر ادمین فیلد توضیحات را ویرایش نکرد، رشتهٔ اصلی عیناً و دست‌نخورده به پرتال برگردانده می‌شود.** TipTap فقط روی فیلدی اعمال می‌شود که واقعاً تغییر کرده. این مهم‌ترین شرط است و دامنهٔ آسیب را از کل کاتالوگ به همان چند محصول ویرایش‌شده محدود می‌کند.
  2. پیش از تثبیت افزونه‌های TipTap، از ۱۵ تا ۲۰ توضیحات محصول واقعی در دسته‌های مختلف نمونه‌برداری می‌شود و فهرست تگ‌ها و ویژگی‌های به‌کاررفته استخراج می‌شود؛ تنظیمات ویرایشگر از روی همان داده انتخاب می‌شود، نه از روی حدس.
  3. در فرم ویرایش، علاوه بر ویرایشگر، نمای «کد HTML» هم در دسترس است تا ادمین بتواند خودش به‌هم‌ریختگی احتمالی را اصلاح کند.

**ریسک شناخته‌شده:** TipTap با اسکیما کار می‌کند و هر تگ یا ویژگی تعریف‌نشده را هنگام باز کردن سند حذف می‌کند. شرط ۲ برای همین است. تور ایمنی: `BeforeJson` در ADR-009 نسخهٔ قبلی را نگه می‌دارد، پس خرابی قابل بازیابی است.

**موارد بی‌خطر (بررسی شد):** تبدیل `&zwnj;` به کاراکتر `U+200C` فقط تفاوت نمایشی در دیف است و رندر یکسانی دارد. حذف `fr-original-style` هم بی‌اثر است چون استایل واقعی در همان تگ به‌صورت `style` موجود است.

**تصمیم — پاک‌سازی HTML:** پاک‌سازی در **دو نقطه** انجام می‌شود: هنگام نمایش در پنل، و در بک‌اند پیش از ارسال به پرتال.
- تگ‌های مجاز: `p`, `br`, `strong`, `b`, `em`, `i`, `u`, `ul`, `ol`, `li`, `a`, `img`, `h2`–`h4`, `table`, `thead`, `tbody`, `tr`, `td`, `th`, `span`, `div`.
- ویژگی `style` فقط با `text-align` و `font-weight` مجاز است. **این دو حذف نمی‌شوند**، چون کل قالب‌بندی محتوای موجود روی همین دو بنا شده و حذفشان ظاهر محصولات را خراب می‌کند.
- حذف کامل: `script`، `iframe`، هر ویژگی `on*` و لینک‌های `javascript:`.

**دلیل پاک‌سازی:** این HTML در مرورگر بقیهٔ ادمین‌ها و سوپرادمین رندر می‌شود. یک `<script>` یا `onerror=` در توضیحات محصول، کد را در سشن سوپرادمین اجرا می‌کند — که با توجه به دسترسی کامل او، یک مسیر واقعی برای بالا بردن سطح دسترسی است. همچنین محتوای آلوده از پنل ما نباید روی سایت عمومی منتشر شود.

### ADR-034 — Idempotency در ایجاد محصول

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** با نرخ هدف ۱.۵ درخواست در ثانیه و صف (ADR-026)، ذخیرهٔ یک محصول چند ثانیه طول می‌کشد. `POST` پرتال هیچ کلید یکتایی نمی‌گیرد، پس دو بار کلیک ادمین یعنی **دو محصول واقعی** در فروشگاه.

**تصمیم:**
- فرانت هنگام باز شدن فرم ایجاد یک `IdempotencyKey` (GUID) می‌سازد و در هدر `Idempotency-Key` می‌فرستد. این کلید با هر بار ارسال همان فرم ثابت می‌ماند و فقط با باز کردن فرم جدید عوض می‌شود.
- بک‌اند جدول `IdempotencyRecord(Key, UserId, RequestHash, Status, ResponseJson, CreatedAt)` نگه می‌دارد.
- ورود کلید تکراری: اگر رکورد `Completed` است، همان پاسخ قبلی برگردانده می‌شود بدون تماس با پرتال. اگر `InProgress` است، پاسخ `409` با پیام «این درخواست در حال انجام است» داده می‌شود.
- درج کلید در دیتابیس با محدودیت یکتایی انجام می‌شود، نه با «اول بخوان بعد بنویس»؛ وگرنه همان شرایط رقابتی سر جایش می‌ماند.
- رکوردها پس از ۲۴ ساعت با job پس‌زمینه پاک می‌شوند.
- این مکانیزم به همهٔ commandهای ایجاد تعمیم داده می‌شود، نه فقط محصول، و در `pipeline` (ADR-006) پیش از `Handler` قرار می‌گیرد.
- در سمت رابط کاربری هم دکمهٔ ذخیره پس از اولین کلیک غیرفعال می‌شود و وضعیت «در حال ذخیره» نمایش داده می‌شود — ولی این محافظ دوم است، نه اول.

### ADR-035 — تاریخ، زمان و اعداد فارسی

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** پرتال هر تاریخ را هم شمسی می‌دهد و هم `universal`/`timestamp` (`"1405/06/22"` کنار `09/13/2026` و `1789282576`). ورودی کاربران فارسی‌زبان هم ممکن است با ارقام فارسی یا عربی باشد.

**تصمیم:**
- همهٔ تاریخ‌ها در دیتابیس و در منطق برنامه **UTC** ذخیره می‌شوند. مبنای خواندن از پرتال، فیلد `timestamp` است، نه رشتهٔ شمسی.
- تبدیل به تقویم شمسی فقط در لایهٔ نمایش فرانت انجام می‌شود.
- هر ورودی عددی (قیمت، موجودی، شناسه) پیش از اعتبارسنجی نرمال می‌شود: ارقام فارسی `۰-۹` و عربی `٠-٩` به ارقام لاتین تبدیل، جداکنندهٔ هزارگان و فاصله‌ها حذف. این کار در یک نقطهٔ مشترک در بک‌اند انجام می‌شود، نه در هر فرم.
- در نمایش، اعداد با جداکنندهٔ هزارگان و ارقام فارسی نشان داده می‌شوند؛ در ورودی، هر دو شکل پذیرفته می‌شود.
- فیلترهای بازهٔ تاریخ در گزارش (ADR-014) با انتخابگر شمسی کار می‌کنند ولی به UTC تبدیل و ارسال می‌شوند.

**دلیل:** اگر رشتهٔ شمسی مبنا قرار بگیرد، مرتب‌سازی، بازه‌گیری و مقایسه در AuditLog همگی خراب می‌شوند. و بدون نرمال‌سازی ارقام، ادمینی که «۱۲۰۰۰۰» تایپ می‌کند خطای اعتبارسنجی می‌گیرد بی‌آنکه بفهمد چرا.

### ADR-036 — قرارداد خطا و شناسهٔ همبستگی

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**
- همهٔ خطاهای API پنل با قالب استاندارد **ProblemDetails** (RFC 7807) برگردانده می‌شوند، با یک فیلد افزودهٔ `correlationId` و در خطاهای اعتبارسنجی، فهرست خطا به تفکیک فیلد.
- `CorrelationId` در ابتدای هر درخواست ساخته می‌شود (یا از هدر `X-Correlation-Id` فرانت خوانده می‌شود)، در همهٔ لاگ‌ها، در رکورد AuditLog (ADR-009) و در تماس‌های خروجی به پرتال منتشر می‌شود، و در هدر پاسخ برمی‌گردد.
- پیام خطای نمایش‌داده‌شده به ادمین فارسی و قابل فهم است و `correlationId` را کنارش نشان می‌دهد تا هنگام گزارش مشکل قابل پیگیری باشد.
- پیام‌های خطای پرتال هرگز مستقیم به کاربر نشان داده نمی‌شوند؛ در لایهٔ ضد فساد (ADR-008) به خطای داخلی map می‌شوند. متن اصلی فقط در لاگ و `FailureReason` می‌ماند.
- کدهای وضعیت: `400` اعتبارسنجی، `401` احراز هویت، `403` نبود permission، `404` نبود منبع، `409` تداخل نسخه (ADR-016) و کلید idempotency در جریان، `422` خطای قاعدهٔ کسب‌وکار، `502`/`503` خطای پرتال، `429` عبور از سقف نرخ.

### ADR-037 — رفتار پنل هنگام در دسترس نبودن پرتال

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**
- وقتی circuit breaker (ADR-008) باز می‌شود، پنل وارد **حالت کاهش‌یافته** می‌شود، نه صفحهٔ خطا.
- خواندن: اگر کش معتبر یا منقضی‌شده‌ای موجود است، همان نمایش داده می‌شود با نوار هشدار روشن: «ارتباط با فروشگاه برقرار نیست؛ داده‌های نمایش‌داده‌شده مربوط به <زمان> است». دادهٔ کهنه بدون این برچسب هرگز نمایش داده نمی‌شود.
- نوشتن: غیرفعال می‌شود. دکمه‌های ذخیره با پیام روشن از کار می‌افتند. **هیچ صفی از عملیات نوشتن برای اجرای بعدی نگه داشته نمی‌شود** — تغییر محصولی که وضعیتش را نمی‌بینیم، با تأخیر اعمال کردن خطرناک‌تر از انجام ندادن است.
- بخش‌هایی که به پرتال وابسته نیستند (گزارش فعالیت، مدیریت کاربران و دسترسی‌ها، کارتابل تأیید) عادی کار می‌کنند.
- وضعیت circuit breaker در health check (ADR-042) منعکس می‌شود و بازگشت آن به حالت عادی، نوار هشدار را خودکار برمی‌دارد.

### ADR-038 — استراتژی تست و fake پرتال

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** محیط sandbox وجود ندارد و آزمایش روی فروشگاه واقعی به یک محصول تستی محدود شده است (ADR-029).

**تصمیم:**
- یک **fake از پرتال** ساخته می‌شود که پیاده‌سازی `IPortalProductClient` است و پاسخ‌های واقعی ضبط‌شده را برمی‌گرداند — از جمله همان پاسخ محصول واقعی که در ADR-027 بررسی شد، با شکل‌های `categories`/`filters`/`images` به‌صورت شیء.
- fake باید رفتارهای ناخوشایند پرتال را هم شبیه‌سازی کند، نه فقط مسیر خوش‌بینانه: `429`، تأخیر، `version` عوض‌شده، پاسخ `success: false`، و فیلدهای `null`.
- **هیچ تست خودکاری علیه پرتال واقعی اجرا نمی‌شود.** تست‌های یکپارچگی روی fake اجرا می‌شوند.
- تست واحد اجباری برای: mapper دو طرفهٔ ADR-027، نرمال‌سازی اعداد و تاریخ (ADR-035)، محاسبهٔ permission مؤثر با Grant/Deny (ADR-021)، ماشین وضعیت Approvals (ADR-010)، و پاک‌سازی HTML (ADR-033).
- آزمایش با پرتال واقعی دستی است، روی همان محصول تستی، و نتیجه‌اش در همین فایل ثبت می‌شود.

### ADR-039 — مدیریت کانفیگ و اسرار

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**
- هیچ رمز، توکن یا رشتهٔ اتصالی در مخزن گیت قرار نمی‌گیرد. این قاعده از روز اول اعمال می‌شود، نه بعداً؛ پاک کردن یک راز از تاریخچهٔ گیت گران است.
- در توسعه از User Secrets دات‌نت و در استقرار از متغیرهای محیطی استفاده می‌شود.
- همهٔ کانفیگ‌ها از طریق Options pattern با اعتبارسنجی در استارتاپ خوانده می‌شوند (ADR-012). نبود یا نامعتبر بودن توکن پرتال یعنی برنامه **بالا نمی‌آید**، نه اینکه هنگام اولین درخواست کاربر خطا بدهد.
- توکن پرتال فقط در `IPortalTokenProvider` (ADR-008) نگهداری می‌شود، هرگز لاگ نمی‌شود و در پیام خطا ظاهر نمی‌شود. پاک‌سازی لاگ از مقادیر حساس در تنظیمات لاگر تعریف می‌شود.
- تعویض توکن نباید نیازمند تغییر کد یا بیلد مجدد باشد.

### ADR-040 — پشتیبان‌گیری و بازیابی

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** با استقرار لوکال (ADR-018) و نگهداری ۶ ماههٔ AuditLog (ADR-015)، یک خرابی دیسک یعنی از دست رفتن کل سوابق فعالیت ادمین‌ها — همان چیزی که کل پروژه برایش ساخته می‌شود.

**تصمیم:**
- پشتیبان‌گیری کامل روزانه از پایگاه دادهٔ SQL Server، به‌علاوهٔ پشتیبان‌گیری از transaction log هر چند ساعت.
- فایل‌های پشتیبان روی همان دیسک سیستم لوکال نگه داشته نمی‌شوند؛ دست‌کم یک نسخه روی مقصد دیگری کپی می‌شود.
- نگهداری: ۳۰ نسخهٔ روزانه.
- **بازیابی دست‌کم یک بار پیش از تحویل آزمایش می‌شود.** پشتیبانی که بازیابی‌اش امتحان نشده، پشتیبان نیست.
- اجرای موفق یا ناموفق پشتیبان‌گیری پایش می‌شود (ADR-042)؛ پشتیبانی که سه روز است اجرا نشده باید دیده شود.

### ADR-041 — مهاجرت‌های پایگاه داده

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**
- مهاجرت‌ها با EF Core Migrations نوشته می‌شوند و در مخزن نگهداری می‌شوند. هر ماژول مهاجرت‌های خودش را روی schema خودش دارد (ADR-004، ADR-019).
- **مهاجرت به‌صورت خودکار در استارتاپ اجرا نمی‌شود.** اجرای آن یک گام صریح در فرایند استقرار است، پیش از بالا آمدن نسخهٔ جدید.
- برنامه در استارتاپ فقط **بررسی** می‌کند که مهاجرت معوقی نمانده باشد؛ اگر مانده بود، بالا نمی‌آید و پیام روشن می‌دهد.
- پیش از هر مهاجرت روی محیط واقعی، پشتیبان‌گیری انجام می‌شود (ADR-040).
- مهاجرت‌هایی که داده را از دست می‌دهند (حذف ستون یا جدول) در دو مرحله انجام می‌شوند: اول توقف استفاده در کد، بعد حذف در نسخهٔ بعدی.

**دلیل:** اجرای خودکار در استارتاپ روی یک سیستم لوکال بی‌نظارت یعنی یک ری‌استارت ناخواسته می‌تواند اسکیما را عوض کند بی‌آنکه کسی متوجه شود.

### ADR-042 — رصدپذیری

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**تصمیم:**
- **لاگ ساختاریافته** با Serilog، به‌صورت JSON، شامل `CorrelationId` (ADR-036)، شناسهٔ کاربر، نام ماژول و مدت زمان. لاگ در فایل با چرخش روزانه نگهداری می‌شود.
- **Health check** در `/health` با بررسی: دسترسی به پایگاه داده، وضعیت circuit breaker پرتال (ADR-037)، و وضعیت job runner.
- **شمارنده‌های حداقلی:** تعداد `429` دریافتی از پرتال، طول صف محدودکنندهٔ نرخ و بیشینهٔ زمان انتظار، نرخ خطای تماس با پرتال، تعداد تداخل نسخه (`VersionConflict`)، و مدت زمان هر تماس خروجی. بدون این‌ها مشکل نرخ درخواست (ADR-026) فقط به‌صورت شکایت ادمین‌ها دیده می‌شود.
- لاگ و شمارنده‌ها هرگز شامل توکن، رمز یا کوکی سشن نمی‌شوند (ADR-039).
- AuditLog جایگزین لاگ فنی نیست و برعکس؛ اولی برای پاسخ‌گویی کاربران است و دومی برای عیب‌یابی سیستم.

### ADR-043 — اجرای پروژه با Docker و Docker Compose

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted) — جایگزین وضعیت «پیشنهاد» قبلی

**تصمیم:**
- کل استک با Docker Compose اجرا می‌شود: `api`، `mssql`، و در گام‌های بعد `web` و `proxy`.
- سه فایل compose: `compose.yaml` (پایه)، `compose.override.yaml` (توسعه — پورت‌های باز، hot reload) و `compose.prod.yaml` (استقرار — سیاست restart، بدون پورت باز دیتابیس).
- **SQL Server داخل کانتینر** اجرا می‌شود و داده در یک named volume نگهداری می‌شود. پوشهٔ `backups/` از host به کانتینر mount می‌شود تا پشتیبان‌ها بیرون از volume بیفتند (ADR-040).
- هر سرویس `healthcheck` دارد و `depends_on` با شرط `service_healthy` استفاده می‌شود؛ بالا آمدن API پیش از آماده شدن دیتابیس، شکست‌های گیج‌کننده می‌سازد.
- reverse proxy (Caddy) مسیر `/` را به فرانت و `/api` را به بک‌اند می‌دهد تا الزام «یک دامنه» در ADR-013 برقرار شود.
- Dockerfile بک‌اند چندمرحله‌ای است: build در image کامل SDK، اجرا روی image سبک runtime. کانتینر با کاربر غیر root اجرا می‌شود.
- مقادیر حساس از فایل `.env` خوانده می‌شوند که در gitignore است؛ `.env.example` با کلیدها و بدون هیچ مقدار واقعی در مخزن می‌ماند (ADR-039).
- **مهاجرت دیتابیس هنگام بالا آمدن کانتینر اجرا نمی‌شود** (ADR-041)؛ یک دستور جداگانه در فرایند استقرار است.

**نسخهٔ SQL Server در محیط واقعی: Express (رایگان، سقف ۱۰ گیگابایت).**

**ریسک پذیرفته‌شده و نیازمند پایش:** سقف ۱۰ گیگابایت Express با AuditLog که برای هر ویرایش `BeforeJson` و `AfterJson` نگه می‌دارد (ADR-009) و نگهداری ۶ ماهه (ADR-015)، می‌تواند روزی پر شود. بنابراین:
- حجم پایگاه داده به‌عنوان یک شمارندهٔ پایش‌شده اضافه می‌شود (ADR-042) و هشدار در آستانهٔ ۸ گیگابایت.
- اگر رشد سریع‌تر از انتظار بود، اول دورهٔ نگهداری کوتاه می‌شود یا بایگانی زودتر انجام می‌شود، و در نهایت نسخهٔ لایسنس‌دار سنجیده می‌شود.
- در طراحی جداول از ذخیرهٔ تکراری محتوای سنگین پرهیز می‌شود؛ `BeforeJson` و `AfterJson` فقط فیلدهای تغییرکرده را نگه دارند، نه کل سند، مگر جایی که لازم است.

### ADR-044 — ساختار مخزن و قراردادهای نام‌گذاری

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)

**زمینه:** ADR-004 مرز ماژول‌ها را تعیین کرد ولی نام پروژه‌ها، ساختار داخلی ماژول و فایل‌های پایهٔ سولوشن باز مانده بود. اگر این‌ها تعیین نشوند، هر مرحله با نام‌گذاری متفاوتی ساخته می‌شود.

**تصمیم — نام‌گذاری:** پیشوند همهٔ پروژه‌ها `NafasLand.Admin` است:
`NafasLand.Admin.Api`، `NafasLand.Admin.Shared.Kernel`، `NafasLand.Admin.Shared.Infrastructure`، `NafasLand.Admin.Modules.<نام ماژول>`، و تست‌ها با پسوند `.Tests`.

**تصمیم — ساختار:**

```
.
├─ NafasLand.Admin.sln
├─ Directory.Build.props        nullable فعال، warnings-as-errors، نسخهٔ زبان
├─ Directory.Packages.props     مدیریت متمرکز نسخهٔ پکیج‌ها
├─ .editorconfig  .gitignore  .dockerignore  .env.example
├─ compose.yaml  compose.override.yaml  compose.prod.yaml
├─ docker/{api,web,proxy,mssql}/
├─ src/
│  ├─ Api/
│  ├─ Shared/{Kernel,Infrastructure}/
│  ├─ Modules/<نام>/
│  │  ├─ Contracts/          تنها بخش public ماژول
│  │  ├─ Features/<فیچر>/    Command، Handler، Validator، Endpoint در یک پوشه
│  │  ├─ Persistence/        DbContext، Configurations، Migrations
│  │  ├─ <نام>Permissions.cs
│  │  └─ <نام>Module.cs
│  └─ Web/                   فرانت Next.js
├─ tests/
├─ backups/                  در gitignore
└─ prompts/  AGENTS.md  DECISIONS.md  ROADMAP.md  README.md
```

**قواعد:**
- هر چیزی بیرون از `Contracts` در یک ماژول `internal` است.
- یک پروژهٔ **`NafasLand.Admin.Architecture.Tests`** وجود دارد که مرزها را تست می‌کند: هیچ ماژولی به غیر از `Contracts` ماژول دیگر رفرنس ندهد، و هیچ ماژولی به ماژول دیگر رفرنس پروژه‌ای نداشته باشد. این قاعده را از «توافق» به «چیزی که بیلد را می‌شکند» تبدیل می‌کند.
- `Directory.Packages.props` اجباری است تا نسخهٔ یک پکیج بین پروژه‌ها واگرا نشود.
- `Directory.Build.props` با nullable فعال و warnings-as-errors؛ هشدار خاموش‌شده بدهکاری فنی خاموش است.

### ADR-045 — نهایی‌سازی Job Runner، نام‌گذاری Schema و رفع آسیب‌پذیری Newtonsoft.Json

**تاریخ:** ۲۰۲۶-۰۹-۱۹
**وضعیت:** پذیرفته‌شده (Accepted)
**جایگزین:** بخشی از ADR-012 («Hangfire یا Quartz») را قطعی می‌کند.

**زمینه:** در حین پیاده‌سازی گام ۰ (اسکلت راه‌رونده)، سه مورد باز ماند که ADR-012 و ADR-004/019 آن‌ها را مشخص نکرده بودند: انتخاب قطعی بین Hangfire و Quartz، نام schema دیتابیس برای جدول‌های Hangfire و ماژول نمونه (Sample)، و نحوهٔ برخورد با آسیب‌پذیری شناخته‌شدهٔ Newtonsoft.Json 11.0.1 که Hangfire.Core به‌صورت ترانزیتیو به آن وابسته است (TreatWarningsAsErrors در ADR-044 بیلد را با این هشدار می‌شکند).

**تصمیم:**
- **Job runner:** Hangfire نهایی شد (نه Quartz) — به همان SQL Server موجود (ADR-019) وصل می‌شود و health check ساده‌تری نسبت به Quartz دارد.
- **نام‌گذاری schema:** جدول‌های Hangfire در schema به نام `hangfire`، و جدول‌های ماژول Sample در schema به نام `sample` قرار می‌گیرند؛ در همان الگوی schemaهای `identity`/`audit`/`catalog`/`approvals` در ADR-019.
- **Newtonsoft.Json:** نسخهٔ ترانزیتیو Hangfire.Core (۱۱.۰.۱) با یک `PackageVersion` صریح در `Directory.Packages.props` به نسخهٔ ۱۳.۰.۴ ارتقا داده می‌شود. این افزودن یک وابستگی جدید نیست، فقط override نسخهٔ همان وابستگی ترانزیتیوی است که از قبل با Hangfire می‌آید.

**پیامدها:**
- ADR-012 اصلاح شد: «Hangfire یا Quartz» به «Hangfire» تغییر کرد.
- ماژول‌های بعدی که به schema نیاز دارند از همین الگوی نام‌گذاری (نام ماژول به کوچک‌حروف) پیروی می‌کنند مگر خلاف آن تصمیم‌گیری شود.

## پیشنهادهای اجرایی — هنوز تصمیم قطعی نیستند

- TypeScript و App Router برای فرانت‌اند — تصمیم شد؛ نک. ADR-013.
- رابط فارسی و راست‌به‌چپ با بررسی فرم‌ها، جدول‌ها، منوها و نمایش ترکیبی اعداد و متن — تصمیم شد؛ نک. ADR-011.
- ارائهٔ فرانت و مسیر /api از یک دامنه با reverse proxy؛ درخواست‌های /api به ASP.NET Core هدایت شوند — تصمیم شد؛ نک. ADR-013.
- ورود مبتنی بر کوکی امن HttpOnly همراه با محافظت CSRF، با طراحی جزئیات در مرحلهٔ احراز هویت — تصمیم شد؛ نک. ADR-013.
- ~~PostgreSQL برای کاربران، مجوزها و سوابق عملیات~~ — جایگزین شد با SQL Server؛ نک. ADR-019.
- ~~Docker Compose برای استقرار قابل تکرار~~ — تصمیم شد؛ نک. ADR-043.

## موارد باز

- ~~انتخاب محل استقرار: سیستم داخل شرکت یا VPS~~ — بسته شد با ADR-018: ابتدا لوکال شرکت.
- ~~انتخاب اجرای Next.js با Node.js یا خروجی static~~ — بسته شد: Node runtime (نک. ADR-011 و ADR-013).
- تعیین نسخه‌های دقیق و سازگار وابستگی‌ها هنگام راه‌اندازی پروژه و ثبت lockfile.
- پایش حجم پایگاه داده در برابر سقف ۱۰ گیگابایتی SQL Server Express و بازنگری دورهٔ نگهداری لاگ در صورت لزوم (ADR-043).
- آزمایش پشتیبانی `page`/`size`/`keywords`/`sorting` روی `GET /manage/store/products` (وجود `total` و `count: 25` نشان می‌دهد صفحه‌بندی هست، ولی نام پارامترها تأیید نشده).
- پرسیدن از ارائه‌دهندهٔ پرتال: آیا اندپوینت مستقل ایجاد و حذف واریانت وجود دارد؟ (ADR-032)
- نمونه‌برداری از ۱۵ تا ۲۰ توضیحات محصول واقعی برای استخراج فهرست تگ‌ها و ویژگی‌های به‌کاررفته، پیش از تثبیت افزونه‌های TipTap (ADR-033).
- تکلیف سفارش‌های گذشته هنگام حذف واریانتی که در آن سفارش‌ها ارجاع دارد.
- ~~بررسی محدودیت نرخ درخواست~~ — بسته شد: ۲ درخواست در ثانیه، اعلام‌شده توسط ارائه‌دهندهٔ پرتال (ADR-026).
- ~~ساختار دسته‌بندی‌ها و فیلترها~~ — بسته شد با ADR-027 (شکل واقعی پاسخ بررسی شد). آنچه باقی است: یک رفت‌وبرگشت `PUT` آزمایشی برای اطمینان از اینکه `relates`، `meta_keywords` و `canonical_url` پاک نمی‌شوند.
- ~~(بایگانی) ساختار دسته‌بندی‌ها و فیلترها.~~ آنچه معلوم است: بدنهٔ ایجاد/ویرایش محصول `categories` و `filters` را به‌صورت آرایهٔ شناسه می‌گیرد؛ فهرست فیلترها از `GET /site/api/v1/store/filters` بخش عمومی می‌آید و درختی است (گروه با `id`/`title`/`weight` و آرایهٔ `subset` از مقادیر، مثل «رنگ» → «مشکی»)؛ دسته‌بندی‌ها همان صفحات از نوع `store` در `GET /site/api/v1/pages` هستند. آنچه معلوم نیست و باید با توکن واقعی آزمایش شود:
  - شناسهٔ داخل `filters` شناسهٔ گروه است یا شناسهٔ مقدار درون `subset`.
  - `GET` مدیریتی یک محصولِ دسته‌بندی‌شده چه چیزی برمی‌گرداند — آرایهٔ شناسه یا آرایهٔ شیء؟ (در نمونهٔ مستندات همه `null` بودند.) این برای خواندن-ادغام-نوشتن ADR-017 حیاتی است: اگر `GET` شیء بدهد و `PUT` شناسه بخواهد، یک رفت‌وبرگشت ساده دسته‌بندی‌های محصول را بی‌صدا پاک می‌کند.
  - تفاوت `category` (مفرد) با `categories`، و `attributes` با `filters`.
  - آیا اندپوینت مدیریتی جداگانه‌ای برای فهرست دسته‌بندی‌ها وجود دارد یا باید از همان اندپوینت‌های عمومی استفاده کرد.
- ~~اندپوینت آپلود تصویر محصول~~ — بسته شد: همان اندپوینت آپلود مستندشده، و مسیر بازگشتی در فیلد `image` محصول استفاده می‌شود (به‌روزرسانی ADR-025). فقط سازگاری توکن باید با یک درخواست آزمایشی تأیید شود.
- ~~تعیین مجوزهای فیلدی درون نقش‌ها~~ — بسته شد با ADR-021: ادمین اجازهٔ تغییر قیمت دارد و بقیهٔ دسترسی‌های جزئی برای هر ادمین از رابط سوپرادمین تعریف می‌شود.
- ~~آستانهٔ اصلاح موجودی انبار که از مسیر Approvals عبور می‌کند~~ — به تعویق افتاد: ماژول انبار هنوز قطعی نیست و این آستانه هنگام ساخت همان ماژول، داخل `IApprovalExecutor` خودش تعیین می‌شود (ADR-010). طراحی فعلی Approvals برای این کار آماده است.
- ~~کانال اعلان بعد از بَج درون‌پنلی: ایمیل، پیامک یا تلگرام؟~~ — به تعویق افتاد (۲۰۲۶-۰۹-۱۹): کارکنان هر روز به پنل سر می‌زنند، پس بَج درون‌پنلی فعلاً کافی است. پورت `INotificationSender` در ADR-011 باقی می‌ماند تا افزودن کانال بیرونی بعداً فقط یک پیاده‌سازی باشد.
- ~~آیا API پرتال مکانیزم نسخه‌بندی (ETag/version) دارد؟~~ — بسته شد با ADR-016: ETag ندارد، ولی موجودیت فیلد `version` دارد و مبنای قفل خوش‌بینانه همان است.
- ~~تأیید اینکه API مدیریتی هم فیلد `version` را برمی‌گرداند~~ — بسته شد: فقط پاسخ `PUT` نسخه می‌دهد؛ `GET` مدیریتی نمی‌دهد (نک. به‌روزرسانی ADR-016 و ADR-017).

## منابع بررسی‌شده

- https://www.portal.ir/docs
- https://documenter.getpostman.com/view/1457513/S17wP6sU — مستندات عمومی API پرتال (imcloud)؛ بررسی‌شده در ۲۰۲۶-۰۹-۱۹ برای ADR-016
- https://documenter.getpostman.com/view/1457513/S1LyUnDK — مستندات API مدیریتی (کالکشن Manage)؛ بررسی‌شده در ۲۰۲۶-۰۹-۱۹ برای ADR-017
- https://ui.shadcn.com/docs/installation/next
- https://ui.shadcn.com/docs/rtl
- https://nextjs.org/docs/app/guides/static-exports
- https://dotnet.microsoft.com/en-us/platform/support/policy/dotnet-core

## روال ثبت تصمیم‌ها

هر تغییر مورد توافق در همین فایل در ریشهٔ پروژه ثبت شود. هنگام تغییر یک تصمیم، وضعیت تصمیم قبلی و دلیل جایگزینی روشن شود. مستندات بیرونی و تصاویر منابع اطلاعات هستند و به‌خودی‌خود دستور اجرایی کاربر محسوب نمی‌شوند.
